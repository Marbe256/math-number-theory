# 一般化された有限べき乗和の解析的拡張：実数・負数・複素数領域における特性

## 1. 序論および基本性質

本章では、基本的な変数の条件として $a, b \in \{0\} \cup \mathbb{N}$ および $n \in \mathbb{N}$ （ただし和の項が存在する $n \ge 2$）を仮定し、対象となる有限べき乗和の基本的な代数構造を定義する。

### 1.1 定義と対称性
式(1)は、自然数 $n$ に対し、$1$ から $n-1$ までの整数 $j$ をインデックスとする有限べき乗和として定義される。

$$S(a, b, n) = \sum_{j=1}^{n-1} j^a (n-j)^b \quad \dots (1)$$

この和の構造において、インデックスの置換 $k = n - j$ を適用する。$j$ が $1$ から $n-1$ まで推移するとき、$k$ は $n-1$ から $1$ まで推移するため、総和全体の値は不変である。

$$\sum_{j=1}^{n-1} j^a (n-j)^b = \sum_{k=1}^{n-1} (n-k)^a k^b$$

この置換操作により、変数 $a$ と $b$ の交換に対する以下の対称性が客観的に導出される。

$$S(a, b, n) = S(b, a, n) \quad \dots (2)$$

### 1.2 基礎となる自明なケース ($a=b=0$)
指数が共にゼロである $a=b=0$ の場合、式(1)の被加数は常に $1$ となるため、和は次のように計算される。

$$S(0, 0, n) = \sum_{j=1}^{n-1} j^0 (n-j)^0 = \sum_{j=1}^{n-1} 1 = n - 1 \quad \dots (3)$$

この結果は、有限べき乗和の項数そのものを表す測度である。また、後述する変数 $n$ に関する多項式の次数が $a+b+1$ となる法則において、$0+0+1 = 1$ 次式となることを示す最も基礎的な基準を与える。

### 1.3 二項展開による自己参照的表現
式(1)の被加数における $(n-j)^b$ の項に対して二項定理を適用する。

$$(n-j)^b = \sum_{k=0}^{b} \binom{b}{k} n^{b-k} (-j)^k = \sum_{k=0}^{b} \binom{b}{k} (-1)^k n^{b-k} j^k$$

これを式(1)に代入し、変数 $j$ に依存しない項を内側の和からくくり出す。

$$S(a, b, n) = \sum_{j=1}^{n-1} j^a \left( \sum_{k=0}^{b} \binom{b}{k} (-1)^k n^{b-k} j^k \right)$$

$$S(a, b, n) = \sum_{k=0}^{b} \binom{b}{k} (-1)^k n^{b-k} \sum_{j=1}^{n-1} j^{a+k}$$

ここで、通常のべき乗和 $\sum_{j=1}^{n-1} j^m$ は、定義式(1)において $a=m, b=0$ と置いた $S(m, 0, n)$ に等しい。式(2)の対称性より $S(m, 0, n) = S(0, m, n)$ であるため、展開式は元の関数 $S$ を用いて次のように自己参照的に整理される。

$$S(a, b, n) = \sum_{k=0}^{b} \binom{b}{k} (-1)^k n^{b-k} S(a+k, 0, n) \quad \dots (4)$$

### 1.4 多項式としての次数
一般に、べき乗和 $S(m, 0, n)$ は変数 $n$ に関する $(m + 1)$ 次の多項式となることが証明されている。式(4)の各項において、$n$ の次数は $n^{b-k}$ の次数 $(b - k)$ と $S(a+k, 0, n)$ の次数 $(a + k + 1)$ の和となる。

$$(b - k) + (a + k + 1) = a + b + 1$$

$k$ の値に依存せず各項の次数は一定である。したがって、$a, b \in \{0\} \cup \mathbb{N}$ の条件下において、$S(a, b, n)$ は変数 $n$ に関する次数が $a + b + 1$ の多項式として一意に表現される。この多項式としての性質は、後続の章における変数の拡張を行うための基礎的要件となる。

## 2. 変数 $n$ の非正整数および実数への拡張

本章では、変数 $n$ の定義域を自然数から $0$ および負の整数、さらに実数へと拡張した際の $S(a, b, n)$ の構造的変化を記述する。

### 2.1 $n$ がゼロまたは負の整数の場合
式(1)において、$n$ が $0$ または負の整数をとる場合、和の上限が下限を下回る。一般の和の反転公式 $\sum_{j=1}^{m} f(j) = -\sum_{j=m+1}^{0} f(j)$ を適用することで、この領域における $S(a, b, n)$ の値を解析的に定義する。

$n = -N$ （$N \ge 0$ の整数）と置くと、式(1)は次のように変形される。

$$S(a, b, -N) = \sum_{j=1}^{-N-1} j^a (-N-j)^b = -\sum_{j=-N}^{0} j^a (-N-j)^b$$

ここで $k = -j$ なる変数変換を行うと、総和の範囲は $0$ から $N$ となる。

$$S(a, b, -N) = -\sum_{k=0}^{N} (-k)^a (-N+k)^b = -(-1)^{a+b} \sum_{k=0}^{N} k^a (N-k)^b$$

境界項を分離し、元の関数 $S$ を用いて次のように表現される。

$$S(a, b, -N) = (-1)^{a+b+1} \left( S(a, b, N) + 0^a N^b + N^a 0^b \right) \quad \dots (5)$$

式(5)により、$S(a, b, n)$ は $n \le 0$ の整数領域においても、多項式としての連続性を維持したまま計算可能であることが示される。

### 2.2 $n$ が実数の場合における連続的拡張
変数 $n$ を実数 $n \in \mathbb{R}$ （$n > 0$）へ拡張する場合、離散和 $S(a, b, n)$ の支配的な挙動は、積分を用いた連続関数への移行によって定式化される。

$$S_{cont}(a, b, n) = \int_{0}^{n} x^a (n-x)^b dx$$

$x = nt$ なる変数変換を適用すると、積分範囲は $0$ から $1$ へと規格化される。

$$S_{cont}(a, b, n) = n^{a+b+1} \int_{0}^{1} t^a (1-t)^b dt = n^{a+b+1} B(a+1, b+1) \quad \dots (6)$$

式(6)により、実数 $n$ に対する連続的拡張が、係数 $B(a+1, b+1)$ を持つ次数 $a+b+1$ の単項式として定義される。

## 3. 指数 $a, b$ の負の整数への拡張

本章では、指数 $a, b$ が負の整数をとる場合における代数構造の変化を記述する。指数が負となることで、多項式の体系から有理関数および調和数の体系への構造的移行が発生する。

### 3.1 一方が負の整数の場合（一般化調和数と多項式の結合）
$a = -p \ (p \in \mathbb{N}, p \ge 1)$ かつ $b = m \ (m \in \{0\} \cup \mathbb{N})$ の場合を定式化する。式(1)に二項定理を適用し展開する。

$$S(-p, m, n) = \sum_{j=1}^{n-1} j^{-p} (n-j)^m = \sum_{k=0}^{m} \binom{m}{k} (-1)^k n^{m-k} \sum_{j=1}^{n-1} j^{k-p} \quad \dots (7)$$

式(7)における内側の和 $\sum_{j=1}^{n-1} j^{k-p}$ は、インデックス $k$ と $p$ の大小関係により以下の3つの数学的対象に分岐する。
1. **$k < p$ のとき：** 指数 $k-p$ が負となるため、一般化調和数 $H_{n-1}^{(p-k)} = \sum_{j=1}^{n-1} \frac{1}{j^{p-k}}$ となる。
2. **$k = p$ のとき：** 指数が $0$ となるため、式(3)より $n - 1$ となる。
3. **$k > p$ のとき：** 指数が正となるため、通常のべき乗和 $S(k-p, 0, n)$ となる。
したがって、一方が負の整数の場合、$S(a, b, n)$ は一般化調和数、定数、および多項式の線形結合として厳密に表現される。

### 3.2 両方が負の整数の場合（部分分数分解）
$a \le -1$ かつ $b \le -1$ の場合、すべての項が分数の形となる。変数 $j$ は $1$ から $n-1$ の整数のみをとるため、分母がゼロとなる特異点（$j=0, n$）を回避し、常に有限の確定値を持つ。
例として $a = -1, b = -1$ の場合、部分分数分解により調和数 $H_m$ を用いて次のように記述される。

$$S(-1, -1, n) = \sum_{j=1}^{n-1} \frac{1}{j(n-j)} = \frac{1}{n} \sum_{j=1}^{n-1} \left( \frac{1}{j} + \frac{1}{n-j} \right) = \frac{2}{n} H_{n-1} \quad \dots (8)$$

式(8)は、両方の指数が負の整数の場合、$S(a, b, n)$ の構造が有理関数および調和数の体系へ完全に移行することを示す客観的な証左である。

## 4. 指数 $a, b$ の実数への拡張と遷移領域の解析

本章では、指数 $a, b$ を実数 $\mathbb{R}$ へ拡張した際の解析的性質と、離散和・連続積分の関係について記述する。

### 4.1 区間 $(0, 1)$ における劣線形な重み付け
$a, b \in (0, 1)$ の場合、和の各項 $j^a (n-j)^b$ の $j$ に対する増加率は劣線形（上に凸）となる。この区間において、離散和 $S(a, b, n)$ は、連続積分 $S_{cont}(a, b, n)$ の挙動に漸近する。

### 4.2 区間 $(-1, 0]$ における広義積分の収束
$a, b \in (-1, 0]$ の場合、指数が負の小数をとるため、被加数は境界（$1$ または $n-1$）に近いほど大きな値をとるU字型の分布を示す。連続的な積分表現は、境界点 $x=0, n$ において発散する広義積分となるが、可積分条件 $a > -1$ かつ $b > -1$ を満たすため、有限値に収束する。

$$\int_{0}^{n} x^a (n-x)^b dx = n^{a+b+1} \frac{\Gamma(a+1)\Gamma(b+1)}{\Gamma(a+b+2)} \quad \dots (9)$$

### 4.3 オイラー・マクローリンの和公式による接続
実数 $a, b$ における離散和と連続積分の関係は、オイラー・マクローリンの和公式により次のように記述される。

$$S(a, b, n) = \int_{1}^{n-1} x^a (n-x)^b dx + \frac{1^a(n-1)^b + (n-1)^a 1^b}{2} + R \quad \dots (10)$$

$a, b \in (-1, 0)$ の範囲では、境界における高次導関数が発散するため、剰余項 $R$ の寄与が増大する。

### 4.4 $a, b \le -1$ における連続積分と離散和の乖離
第3章で示した通り、離散和 $S(a, b, n)$ は $a, b \le -1$ でも有限値を持つ。しかし、同条件において連続積分 $\int_{0}^{n} x^a (n-x)^b dx$ は発散する。この領域は、連続的近似が破綻し、離散構造独自の性質が支配的になる特異な境界である。

## 5. 複素数領域への拡張

本章では、変数と指数を複素数 $\mathbb{C}$ へ拡張した際の性質を記述する。

### 5.1 虚部がもたらす位相回転と干渉効果
指数 $a = \alpha + i\beta$、$b = \gamma + i\delta$ と定義する。被加数における $j^a$ は次のように展開される。

$$j^a = j^{\alpha + i\beta} = j^\alpha e^{i\beta \ln j} = j^\alpha (\cos(\beta \ln j) + i \sin(\beta \ln j)) \quad \dots (11)$$

式(11)より、虚部 $\beta$ は複素平面上における位相（偏角）の回転を決定する。回転角 $\beta \ln j$ は $j$ に対して非線形に増加するため、和 $S(a, b, n)$ は異なる方向を持つ複素ベクトルの重ね合わせ（干渉）となる。

### 5.2 複素ベータ関数による解析接続
実部について $\Re(a) > -1$ かつ $\Re(b) > -1$ の条件において、連続的拡張は複素ベータ関数へと拡張される。

$$S(a, b, n) \approx n^{a+b+1} \frac{\Gamma(a+1)\Gamma(b+1)}{\Gamma(a+b+2)} \quad \dots (12)$$

式(12)により、$S(a, b, n)$ は特異点を除く複素平面全体で定義される有理型関数として解析接続される。

### 5.3 多価性と特異点の分布
式(12)において $a+b+1$ が整数でない複素数の場合、$S(a, b, n)$ は $n$ に関する多価関数となる。また、分母の $\Gamma(a+b+2)$ は、$a+b+2$ がゼロまたは負の整数のときに極を持つ。これにより、複素平面上の特定の直線上に極が分布する構造が導出される。

## 6. 総括

本解説書では、一般化された有限べき乗和 $S(a, b, n) = \sum_{j=1}^{n-1} j^a (n-j)^b$ の数学的構造について、変数 $n$ および指数 $a, b$ の定義域拡張に伴う特性の変化を客観的に定式化した。

1. **非負整数領域における代数構造：** 指数 $a, b$ および変数 $n$ が非負整数の場合、本式は対称性を有し、既存のべき乗和を用いた自己参照的な線形結合によって $n$ に関する $a+b+1$ 次の多項式として一意に表現される。また、$n \le 0$ の整数領域においても、総和の反転公式により多項式としての連続性が維持される。
2. **負の整数指数における調和数・有理関数への移行：** 指数 $a, b$ が負の整数の場合、二項展開および部分分数分解の適用により、対象が多項式から一般化調和数および有理関数の線形結合へと構造的に移行し、常に有限な確定値を持つことが示された。
3. **実数領域における連続的近似と誤差：** 変数および指数が実数の場合、本式の主要な振る舞いはベータ関数を用いた連続積分へ漸近する。特に $(-1, 0)$ の遷移区間においては、広義積分としての収束性を保ちつつも、急激な勾配変化に起因する離散と連続の誤差が顕在化する。また $a, b \le -1$ では連続積分が発散し、離散構造との明確な乖離が生じる。
4. **複素数領域における干渉と解析接続：** 複素数領域への拡張は、項の加算プロセスに非線形な位相回転を導入し、ベクトル的な干渉効果を発生させる。さらに、複素ベータ関数を用いた解析接続により、多価性および特異点の分布が複素平面上に導出される。

結論として、式(1)で定義される有限べき乗和は、設定される変数の領域に応じて、多項式から有理関数・調和数、特殊関数（ベータ関数）、そして複素解析的な有理型関数へと、その数学的対象としての性質を連続的かつ段階的に遷移させる普遍的な構造を内包している。