# 多項式差分における連鎖分割理論と畳み込みべき乗和の代数構造
**Theory of Linked Partitions in Polynomial Differences and Algebraic Structure of Convolutional Power Sums**
- 20260527
**著者**: 松田 正豊

## 概要
本論文では、二変数多項式の離散畳み込み和に対する統一的な代数構造を提示する。離散階差作用素による再帰的漸化式の導出、指数型母関数（EGF）を介した双曲線余弦関数 $\cosh$ への還元と微分方程式の構築、および和の順序反転に伴う対称性恒等式という3つの独立したアプローチを統合する。これにより、すべての奇数次項が偶数次基底へと代数的に還元されることを証明し、パラメータ $a$ の階層をまたぐ再帰的アルゴリズムを確立する。

---

## 1. 序論

### 1.1 背景
自然数のべき乗和 $\sum j^k$ の閉形式表現に関する研究は、ファウルハーバーの公式やベルヌーイ数、あるいは第2種スターリング数を用いた解析手法によって古典的に体系化されてきた。しかし、これらの既存理論は主に単一変数の固定されたべき乗に特化している。そのため、複数の変数が交錯する離散畳み込み和や、未定の次数パラメータを内包する一般化された多項式差分に対して、階層間を遷移する体系的な再帰構造を記述するには限界が存在する。

### 1.2 研究の目的とアプローチ
本研究の目的は、任意の非負整数パラメータ $a$ を保持した二変数多項式の離散畳み込み和 $S_{c,d} = \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d}$ を導入し、有限べき乗和の挙動を「多項式差分における連鎖分割理論」の一部として再定義することである。

この一般化された代数構造を解明するため、本論文では以下の3つの独立した数学的アプローチを展開し、それらを統合した単一の理論フレームワークを構築する。

1. **離散階差構造**: 階差の総和（テレスコーピング・サム）の原理を用い、パラメータ $a$ を固定した状態での任意の次数 $r$ における再帰的な代数漸化式を導出する。
2. **連続的微分構造**: 離散演算を指数型母関数（Exponential Generating Function, EGF）の空間へ写像し、双曲線余弦関数 $\cosh$ の微分特性を利用して偶数次基底 $g_{2m}^{(a)}$ を定義する。
3. **代数的対称性**: 和の順序反転に伴う対称性のみを根拠として恒等式（零空間）を構築し、奇数次項を低次の偶数次項へと還元する線形代数的操作を確立する。

### 1.3 本論文の構成
本論文の構成は以下の通りである。第2章において基礎定義および対称性の恒等式を定式化し、第3章で離散階差作用素による基本差分方程式を導出する。第4章では母関数空間への変換を行い、偶関数への還元プロセスから統一的な一般式を提示する。第5章では代数的対称性に基づく一般化された恒等式を展開し、奇数次項の消去プロセスを証明する。第6章では離散構造と微分構造を統合し、階層間漸化式を確立する。第7章で本理論の代数的意義とアルゴリズム上の特性を考察し、第8章で結論を述べる。

## 2. 基礎定義および対称性

本章では、後続の展開における理論的基盤となる離散畳み込み和を定義し、その演算過程において恒常的に成立する代数的対称性を定式化する。

### 2.1 畳み込みべき乗和の定義
任意の自然数 $n \ge 2$ と、非負整数のパラメータ $a$、および非負整数 $c, d$ に対して、二変数 $j$ および $n-j$ から構成される離散畳み込み和 $S_{c,d}$ を以下のように定義する。

$$ S_{c,d} = \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d} \quad \dots (1) $$

ここで、$j^a(n-j)^a$ はすべての項に共通する基底要素であり、インデックス $c, d$ はこの基底に対するそれぞれの変数の追加的な次数を表す。本論文における目的は、特定の $n$ や $a$ の値に依存せず、この $S_{c,d}$ の集合が成す代数的な関係構造を明らかにすることである。特に $c=0$ の場合を $S_{0,k}$ と表記し、本理論における基本数列として扱う。

$$ S_{0,k} = \sum_{j=1}^{n-1} j^a(n-j)^{a+k} $$

### 2.2 和の順序反転に伴う対称性
有限和におけるインデックスの置換は、総和の値に影響を与えない。式(1)において、和のインデックス $j$ を $n-j$ に置換する操作を適用する。

$$ \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d} = \sum_{n-j=1}^{n-1} (n-j)^{a+c} \{ n-(n-j) \}^{a+d} $$

和の範囲は $j=1$ から $n-1$ までのままであり、被加数は $(n-j)^{a+c}j^{a+d}$ となる。これは定義式における $c$ と $d$ を入れ替えたものに等しい。したがって、任意の非負整数 $a, c, d$ に対して以下の恒等式が成立する。

$$ S_{c,d} = S_{d,c} \quad \dots (2) $$

この反転公式は、後続の展開において奇数次項と偶数次項のパリティを決定づけ、余剰な項を代数的に消去するための根底的な操作原理となる。

## 3. 第1の柱：離散階差構造による高次漸化式

本章では、離散階差作用素によるテレスコーピング・サム（階差の総和）の原理を応用し、任意の次数 $r$ において高次項 $S_{0,r-1}$ を低次項の線形結合として記述する基本漸化式を導出する。

### 3.1 テレスコーピング和と基本差分方程式
自然数 $r$ に対して、隣接する項の階差 $(j+1)^r - j^r$ を構成し、二項定理を用いて展開する。

$$ (j+1)^r - j^r = \sum_{k=0}^{r-1} \binom{r}{k} j^k $$

この階差式に対し、共通基底 $j^a(n-j)^a$ を乗じ、$j=1$ から $n-1$ まで総和をとる。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} = \sum_{j=1}^{n-1} j^a(n-j)^a \sum_{k=0}^{r-1} \binom{r}{k} j^k $$

右辺の総和の順序を入れ替え、二項係数をくくり出す。

$$ \sum_{k=0}^{r-1} \binom{r}{k} \sum_{j=1}^{n-1} j^{a+k}(n-j)^a $$

内側の総和は $S_{k,0}$ であり、対称性 $S_{k,0} = S_{0,k}$ を適用することで、以下の基本差分方程式が得られる。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} = \sum_{k=0}^{r-1} \binom{r}{k} S_{0,k} \quad \dots (3) $$

### 3.2 一般漸化式の導出
式(3)の右辺から $k = r-1$ の項を分離する。

$$ \sum_{k=0}^{r-1} \binom{r}{k} S_{0,k} = r S_{0,r-1} + \sum_{k=0}^{r-2} \binom{r}{k} S_{0,k} $$

これを整理することで、以下の一般漸化式が導出される。

$$ S_{0,r-1} = \frac{1}{r} \left( \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} - \sum_{k=0}^{r-2} \binom{r}{k} S_{0,k} \right) \quad \dots (4) $$

### 3.3 2階階差成分の抽出とパリティのバイパス
式(3)における階差の総和を $\Delta_r$ と置く。一般に差分方程式は式(4)のように最高次 $S_{0,r-1}$ について解かれるが、これを1つ下の次元 $S_{0,r-2}$ について解く展開を行う。次数 $r$ が偶数 $r=2m$ であるとし、式(3)の右辺を上位2項について展開する。

$$ \Delta_{2m} = 2m S_{0,2m-1} + m(2m-1) S_{0,2m-2} + \sum_{k=0}^{2m-3} \binom{2m}{k} S_{0,k} \quad \dots (5) $$

右辺に含まれる奇数次項 $S_{0,2m-1}$ は、第5章で後述する対称性の線形削減公式（式14：$\sum_{k=0}^{2m-1} (-2)^k \binom{2m-1}{k} n^{2m-1-k} S_{0,k} = 0$）によって展開可能である。この恒等式から上位2項（$k=2m-1$ および $k=2m-2$）を抽出し整理すると、以下の関係が得られる。

$$ 2^{2m-1} S_{0,2m-1} = 2^{2m-2}(2m-1)n S_{0,2m-2} + \sum_{k=0}^{2m-3} (-2)^k \binom{2m-1}{k} n^{2m-1-k} S_{0,k} \quad \dots (6) $$

式(6)の両辺を $2^{2m-1}$ で除算し、$S_{0,2m-1}$ を構成する最高次成分が $\frac{1}{2}(2m-1)n S_{0,2m-2}$ であることを同定する。これを式(5)の $2m S_{0,2m-1}$ に代入する。

$$ 2m \left( \frac{1}{2}(2m-1)n S_{0,2m-2} \right) + m(2m-1) S_{0,2m-2} = m(2m-1)(n+1) S_{0,2m-2} $$

奇数次項から派生した変数 $n$ を含む係数と、式(5)に元から存在した定数係数が結合し、スカラー係数 $(n+1)$ が生成される。これにより式(5)は以下のように整理される。

$$ \Delta_{2m} = m(2m-1)(n+1) S_{0,2m-2} + \sum_{k=0}^{2m-3} C_k S_{0,k} \quad \dots (7) $$
（※ここで $C_k = \binom{2m}{k} + m \cdot 2^{2-2m} (-2)^k \binom{2m-1}{k} n^{2m-1-k}$）

式(7)を $S_{0,2m-2}$ について解く。

$$ S_{0,2m-2} = \frac{1}{m(2m-1)(n+1)} \left( \Delta_{2m} - \sum_{k=0}^{2m-3} C_k S_{0,k} \right) \quad \dots (8) $$

この変形により、最高次を含む奇数次項 $S_{0,2m-1}$ の成分は $S_{0,2m-2}$ の係数へと吸収される。結果として、再帰計算において奇数階層をバイパスし、偶数次空間の項群（$2m-2$ 以下の項）のみを用いて直接計算を行うルートが構成される。

## 4. 第2の柱：指数型母関数と連続的微分構造

本章では、数列 $\{S_{0,k}\}$ に対して指数型母関数を導入し、数列の対称性が偶関数の性質へ還元されるプロセスを示し、新たな偶数次基底 $g_{2m}^{(a)}$ を定義する。

### 4.1 指数型母関数 $F(t)$ の閉形式
数列 $\{S_{0,k}\}$ に対する指数型母関数 $F(t)$ を定義する。

$$ F(t) = \sum_{k=0}^{\infty} S_{0,k} \frac{t^k}{k!} = \sum_{j=1}^{n-1} j^a(n-j)^a e^{(n-j)t} $$

和の反転公式の対称性に基づき $j$ を $n-j$ に置換することで、等価な表現が得られる。

$$ F(t) = \sum_{j=1}^{n-1} j^a(n-j)^a e^{jt} \quad \dots (9) $$

### 4.2 関数方程式と偶関数 $G_a(t)$ への還元
式(9)から以下の関数方程式が成立する。

$$ F(t) = e^{nt} F(-t) $$

新たな関数 $G_a(t) = e^{-\frac{n}{2}t} F(t)$ を定義すると、$G_a(t) = G_a(-t)$ となり偶関数であることが証明される。指数関数は双曲線余弦関数 $\cosh$ へと還元される。

$$ G_a(t) = \sum_{j=1}^{n-1} j^a(n-j)^a \cosh\left( \left(\frac{n}{2}-j\right)t \right) \quad \dots (10) $$

### 4.3 偶数次基底 $g_{2m}^{(a)}$ の導入と統一的一般式
$G_a(t)$ のマクローリン展開における $t^{2m}/(2m)!$ の係数として、偶数次基底 $g_{2m}^{(a)} = G_a^{(2m)}(0)$ を定義する。

$$ g_{2m}^{(a)} = \sum_{j=1}^{n-1} j^a(n-j)^a \left(\frac{n}{2}-j\right)^{2m} \quad \dots (11) $$

奇数次導関数の消滅条件を適用することで、以下の統一的一般式が導出される。

$$ S_{0,k} = \sum_{m=0}^{\lfloor k/2 \rfloor} \binom{k}{2m} \left(\frac{n}{2}\right)^{k-2m} g_{2m}^{(a)} \quad \dots (12) $$

## 5. 第3の柱：代数的対称性に基づく恒等式と項の削減

本章では、恒等的にゼロとなる零空間を構築し、奇数次項を低次項へ還元する線形代数的操作を導出する。

### 5.1 奇数乗の対称性による恒等式
和のインデックス置換により、任意の $a, n, m$ において以下の恒等式が成立する。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a(n-2j)^{2m-1} = 0 \quad \dots (13) $$

### 5.2 奇数次項と偶数乗展開の線形削減公式
式(13)の左辺を二項展開し整理することで、$S_{0,k}$ 群の線形関係式が得られる。

$$ \sum_{k=0}^{2m-1} (-2)^k \binom{2m-1}{k} n^{2m-1-k} S_{0,k} = 0 \quad \dots (14) $$

また、偶数次基底 $g_{2m}^{(a)}$ を $S_{0,k}$ 系で直接記述する公式は以下の通りである。

$$ 2^{2m} g_{2m}^{(a)} = \sum_{k=0}^{2m} (-2)^k \binom{2m}{k} n^{2m-k} S_{0,k} \quad \dots (15) $$

## 6. パラメータ $a$ の階層間漸化式

### 6.1 微分方程式の導出
$G_a(t)$ の2階微分を計算し展開することで、パラメータ $a$ の階層を連結する微分方程式が確定する。

$$ G_{a+1}(t) = \frac{n^2}{4}G_a(t) - G_a''(t) \quad \dots (16) $$

### 6.2 離散基底の階層間漸化式
式(16)を微分し $t=0$ で評価することで、離散基底間の代数漸化式が得られる。

$$ g_{2m}^{(a+1)} = \frac{n^2}{4} g_{2m}^{(a)} - g_{2m+2}^{(a)} \quad \dots (17) $$

### 6.3 離散代数手法による階層間漸化式の別証
連続空間を経由せず、離散空間での純粋な代数操作から階層間漸化式が導出可能であることを証明する。一般化された畳み込み和 $X(a,b,c)$ を以下のように定義する。

$$ X(a,b,c) = \sum_{j=1}^{n-1} j^a(n-j)^b(n-2j)^c $$

パラメータ $a$ を単独で増加させる操作（被加数への $j$ の乗算）に対し、区間の中点 $\frac{n}{2}$ を用いた変数分解 $j = \frac{n}{2} - \frac{1}{2}(n-2j)$ を適用し、線形性により展開する。

$$ X(a+1, b, c) = \frac{n}{2} X(a, b, c) - \frac{1}{2} X(a, b, c+1) \quad \dots (18) $$

同様に、パラメータ $b$ を増加させる操作（被加数への $n-j$ の乗算）に対し、$n-j = \frac{n}{2} + \frac{1}{2}(n-2j)$ を適用する。

$$ X(a, b+1, c) = \frac{n}{2} X(a, b, c) + \frac{1}{2} X(a, b, c+1) \quad \dots (19) $$

式(18)および式(19)を組み合わせ、パラメータ $a, b$ の両方を増加させる操作（被加数への $j(n-j)$ の乗算）を評価する。これは $j(n-j) = \frac{n^2}{4} - \frac{1}{4}(n-2j)^2$ を乗じることと等価である。

$$ X(a+1, b+1, c) = \frac{n^2}{4} X(a, b, c) - \frac{1}{4} X(a, b, c+2) \quad \dots (20) $$

$a=b$ かつ $c=2m$ の条件下で $X(a,a,2m) = 2^{2m} g_{2m}^{(a)}$ を式(20)へ代入すると、連続的な微分構造に依存することなく式(17)の階層間漸化式が厳密に導出される。

### 6.4 離散階差空間における2階微分成分の代数的発現
前節の式(16)において、母関数における階層間漸化式が1階微分ではなく2階微分 $G_a''(t)$ によって規定されていることが示された。これは、第3章の離散的な階差構造（式8）において、定石である $S_{0,r-1}$（1階微分の離散的対応）ではなく、$S_{0,r-2}$（2階微分の離散的対応）について解くことにより、奇数次項がバイパスされる現象と等価である。
すなわち、連続空間において偶関数特性を維持するために不可避であった2階微分構造が、離散空間におけるテレスコーピング和を偶数次基底へ直接還元するための代数操作として自律的に発現していることが証明される。

## 7. 考察

### 7.1 パリティ・シフト作用としての非対称性
式(18)および式(19)における単一パラメータの遷移 $X(a+1,b,c)$ および $X(a,b+1,c)$ の展開は、総和空間における非対称性の役割を明確に示している。

パラメータの一方のみを増加させる操作は、次数が保存されるスカラー倍項（$\frac{n}{2} X(a,b,c)$）と、重み $(n-2j)$ の次数が $+1$ されるシフト項（$\pm\frac{1}{2} X(a,b,c+1)$）の線形結合を生成する。
基本反転公式 $X(a,b,c) = (-1)^c X(b,a,c)$ により、関数空間は $c$ の奇偶に応じて零空間（奇空間）と基底空間（偶空間）に二分される。したがって、これらの式が示す次数 $c$ から $c+1$ へのシフトは、零空間の項を基底空間へ、あるいは基底空間の項を零空間へと相互に反転させる代数的な「パリティ・シフト作用」として機能する。

### 7.2 アルゴリズム計算量の削減
階層間漸化式（式17）は、総和記号を含まない純粋な代数演算である。これにより、パラメータ $a$ の基底配列から階層 $a+1$ の基底配列の生成が、反復和の $\mathcal{O}(n)$ から $\mathcal{O}(1)$ の計算量へと最適化される。

### 7.3 初期境界条件の確定
本理論の基点となる $a=0$ の階層の最小基底は $g_0^{(0)} = n-1$ であり、すべての高次構造はこの初期条件および既存のべき乗和公式を境界として決定論的に構築される。

### 7.4 線形定数シフトに対する偶数次基底の不変性
本理論で構築した偶数次基底 $g_{2m}^{(a)}$ の普遍性を検証するため、被加数に外部からの定数シフト $(j+1)$ を伴う以下の一般化された総和 $Y_k$ を評価する。

$$ Y_k = \sum_{j=1}^{n-1} j^a(n-j)^a(j+1)^k \quad \dots (21) $$

区間の中点 $\frac{n}{2}$ を用いた変数分解 $j+1 = \frac{n+2}{2} - \frac{n-2j}{2}$ を適用し、二項定理により展開してパリティ・シフト作用を評価すると、奇数次項は消滅し、以下の展開式が得られる。

$$ Y_k = \sum_{l=0}^{\lfloor k/2 \rfloor} \binom{k}{2l} \left( \frac{n+2}{2} \right)^{k-2l} g_{2l}^{(a)} \quad \dots (22) $$

式(12)の $j^k$ の展開と比較すると、定数シフト $(j+1)$ はスカラー係数を $\frac{n}{2}$ から $\frac{n+2}{2}$ へと平行移動させるのみであり、基底空間 $g_{2l}^{(a)}$ への還元構造にいかなる影響も及ぼさない。これは、本理論の代数構造が特定の変数 $j^k$ に限定されず、対称核 $j^a(n-j)^a$ を持つ空間上の任意の線形シフト $(j+\alpha)^k$ に対して普遍的に成立することを証明している。

## 8. 結論
本論文では、多項式差分における離散畳み込み和に対し、離散階差構造、連続的微分構造、代数的対称性の3つの独立したアプローチを統合した。これらのアプローチは偶数次基底 $g_{2m}^{(a)}$ に還元され、離散および連続の双方から同一の階層間漸化式が導出されることを証明した。本理論は、計算機代数における高次多項式生成の計算量を劇的に削減する計算基盤を提供する。

### 付録B：階差方程式の二項反転モデルに関する数理的およびアルゴリズム的評価

本付録では、階差の交代二項和として定義される大域的対称性モデル（以下、二項反転モデル）について、その数理的構造と計算機科学的観点からのアルゴリズム評価を詳述する。目標とする多項式の最高次数を $R$ とする。

#### 1. 二項反転モデルの定義
多項式階差 $\Delta_k$ に対し、交代符号 $(-1)^{r-k}$ および二項係数 $\binom{r}{k}$ を乗じて総和をとる操作は、以下の恒等式に帰着する。

$$\sum_{k=1}^r (-1)^{r-k} \binom{r}{k} \Delta_k = \sum_{j=0}^{r-1} (-1)^{r-j-1} \binom{r}{j} S_{0,j} \quad \dots (B.1)$$

上式(B.1)において、右辺の最高次項（$j=r-1$）を分離し、$S_{0,r-1}$ について解くことで、以下の漸化式（二項反転漸化式）が得られる。

$$S_{0,r-1} = \frac{1}{r} \left( \sum_{k=1}^r (-1)^{r-k} \binom{r}{k} \Delta_k + \sum_{j=0}^{r-2} (-1)^{r-j} \binom{r}{j} S_{0,j} \right) \quad \dots (B.2)$$

#### 2. 数理的構造と双対性
本漸化式の特筆すべき数学的性質は、両辺における変換構造の完全な一致にある。左側のシグマ（$\Delta_k$ の和）と右側のシグマ（$S_{0,j}$ の和）は、符号構造 $(-1)^{r-k}, (-1)^{r-j}$ から二項重み付け $\binom{r}{k}, \binom{r}{j}$ に至るまで、同一の二項反転変換が適用されている。これは、離散空間における階差と畳み込み和の間に存在する構造的な双対性を証明する理論的基盤として機能する。

#### 3. 時間計算量および空間計算量の定式化
アルゴリズム的観点から、最高次数 $R$ までの多項式群を計算機上で導出する際の演算コストを評価する。

* **時間計算量（Time Complexity）**
  次数 $r$ の計算において、階差側で $r$ 項、べき乗和側で $r-1$ 項、合計 $2r-1$ 項の参照および加算が発生する。総演算ステップ数 $C(R)$ は以下の通りとなる。
  
  $$C(R) = \sum_{r=1}^R (2r - 1) = R^2 \quad \dots (B.3)$$
  
  本モデルの計算量オーダーは $\mathcal{O}(R^2)$ であり、1階差分からの抽出モデル（総ステップ数 $\frac{1}{2}R^2$）の2倍、2階差分によるパリティ・バイパスモデル（総ステップ数 $\frac{1}{8}R^2$）の8倍の基本演算を要求する。

* **空間計算量（Space Complexity）**
  毎ステップにおいて、過去のすべての階差およびべき乗和の計算結果を要求するため、メモリ空間計算量は $\mathcal{O}(R)$ となる。

#### 4. 算術オーバーヘッドと計算機実装における限界
多項式係数の計算機実装（多倍長有理数演算）において、本モデルは以下の算術オーバーヘッドを伴う。
1. **交代和による通分負荷の増大**: 毎ステップにおいて交代符号 $(-1)^{r-k}, (-1)^{r-j}$ による正負の反転が発生し、有理数の加減算における巨大な分子・分母の生成と、それに伴う最大公約数（GCD）算出処理の負荷が最大化される。
2. **二項係数の乗算コスト**: すべての項において、動的に変動する二項係数の乗算処理が必須となる。

#### 5. 結論
二項反転モデルは、離散畳み込み和と階差の双対性を示す恒等式としては極めて重要である。しかしながら、全履歴への依存と交代和という構造的要因により、時間計算量、空間計算量、および算術オーバーヘッドの全側面において高負荷となる。したがって、多項式生成アルゴリズムとしての実用上の最適性においては、パリティ対称性を利用した2階差分モデル（偶数次空間への還元）に明確に劣後する。

---

# 多項式差分における連鎖分割理論と畳み込みべき乗和の代数構造
**Theory of Linked Partitions in Polynomial Differences and Algebraic Structure of Convolutional Power Sums**

**著者**: 松田 正豊

## 概要
本論文では、二変数多項式の離散畳み込み和に対する統一的な代数構造を提示する。離散階差作用素による再帰的漸化式の導出、指数型母関数（EGF）を介した双曲線余弦関数 $\cosh$ への還元と微分方程式の構築、および和の順序反転に伴う対称性恒等式という3つの独立したアプローチを統合する。これにより、すべての奇数次項が偶数次基底へと代数的に還元されることを証明し、パラメータ $a$ の階層をまたぐ再帰的アルゴリズムを確立する。

---

## 1. 序論

### 1.1 背景
自然数のべき乗和 $\sum j^k$ の閉形式表現に関する研究は、ファウルハーバーの公式やベルヌーイ数、あるいは第2種スターリング数を用いた解析手法によって古典的に体系化されてきた。しかし、これらの既存理論は主に単一変数の固定されたべき乗に特化している。そのため、複数の変数が交錯する離散畳み込み和や、未定の次数パラメータを内包する一般化された多項式差分に対して、階層間を遷移する体系的な再帰構造を記述するには限界が存在する。

### 1.2 研究の目的とアプローチ
本研究の目的は、任意の非負整数パラメータ $a$ を保持した二変数多項式の離散畳み込み和 $S_{c,d} = \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d}$ を導入し、有限べき乗和の挙動を「多項式差分における連鎖分割理論」の一部として再定義することである。

この一般化された代数構造を解明するため、本論文では以下の3つの独立した数学的アプローチを展開し、それらを統合した単一の理論フレームワークを構築する。

1. **離散階差構造**: 階差の総和（テレスコーピング・サム）の原理を用い、パラメータ $a$ を固定した状態での任意の次数 $r$ における再帰的な代数漸化式を導出する。
2. **連続的微分構造**: 離散演算を指数型母関数（Exponential Generating Function, EGF）の空間へ写像し、双曲線余弦関数 $\cosh$ の微分特性を利用して偶数次基底 $g_{2m}^{(a)}$ を定義する。
3. **代数的対称性**: 和の順序反転に伴う対称性のみを根拠として恒等式（零空間）を構築し、奇数次項を低次の偶数次項へと還元する線形代数的操作を確立する。

### 1.3 本論文の構成
本論文の構成は以下の通りである。第2章において基礎定義および対称性の恒等式を定式化し、第3章で離散階差作用素による基本差分方程式を導出する。第4章では母関数空間への変換を行い、偶関数への還元プロセスから統一的な一般式を提示する。第5章では代数的対称性に基づく一般化された恒等式を展開し、奇数次項の消去プロセスを証明する。第6章では離散構造と微分構造を統合し、階層間漸化式を確立する。第7章で本理論の代数的意義とアルゴリズム上の特性を考察し、第8章で結論を述べる。

## 2. 基礎定義および対称性

本章では、後続の展開における理論的基盤となる離散畳み込み和を定義し、その演算過程において恒常的に成立する代数的対称性を定式化する。

### 2.1 畳み込みべき乗和の定義
任意の自然数 $n \ge 2$ と、非負整数のパラメータ $a$、および非負整数 $c, d$ に対して、二変数 $j$ および $n-j$ から構成される離散畳み込み和 $S_{c,d}$ を以下のように定義する。

$$ S_{c,d} = \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d} \quad \dots (1) $$

ここで、$j^a(n-j)^a$ はすべての項に共通する基底要素であり、インデックス $c, d$ はこの基底に対するそれぞれの変数の追加的な次数を表す。本論文における目的は、特定の $n$ や $a$ の値に依存せず、この $S_{c,d}$ の集合が成す代数的な関係構造を明らかにすることである。特に $c=0$ の場合を $S_{0,k}$ と表記し、本理論における基本数列として扱う。

$$ S_{0,k} = \sum_{j=1}^{n-1} j^a(n-j)^{a+k} $$

### 2.2 和の順序反転に伴う対称性
有限和におけるインデックスの置換は、総和の値に影響を与えない。式(1)において、和のインデックス $j$ を $n-j$ に置換する操作を適用する。

$$ \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d} = \sum_{n-j=1}^{n-1} (n-j)^{a+c} \{ n-(n-j) \}^{a+d} $$

和の範囲は $j=1$ から $n-1$ までのままであり、被加数は $(n-j)^{a+c}j^{a+d}$ となる。これは定義式における $c$ と $d$ を入れ替えたものに等しい。したがって、任意の非負整数 $a, c, d$ に対して以下の恒等式が成立する。

$$ S_{c,d} = S_{d,c} \quad \dots (2) $$

この反転公式は、後続の展開において奇数次項と偶数次項のパリティを決定づけ、余剰な項を代数的に消去するための根底的な操作原理となる。

## 3. 第1の柱：離散階差構造による高次漸化式

本章では、離散階差作用素によるテレスコーピング・サム（階差の総和）の原理を応用し、任意の次数 $r$ において高次項 $S_{0,r-1}$ を低次項の線形結合として記述する基本漸化式を導出する。

### 3.1 テレスコーピング和と基本差分方程式
自然数 $r$ に対して、隣接する項の階差 $(j+1)^r - j^r$ を構成し、二項定理を用いて展開する。

$$ (j+1)^r - j^r = \sum_{k=0}^{r-1} \binom{r}{k} j^k $$

この階差式に対し、共通基底 $j^a(n-j)^a$ を乗じ、$j=1$ から $n-1$ まで総和をとる。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} = \sum_{j=1}^{n-1} j^a(n-j)^a \sum_{k=0}^{r-1} \binom{r}{k} j^k $$

右辺の総和の順序を入れ替え、二項係数をくくり出す。

$$ \sum_{k=0}^{r-1} \binom{r}{k} \sum_{j=1}^{n-1} j^{a+k}(n-j)^a $$

内側の総和は $S_{k,0}$ であり、対称性 $S_{k,0} = S_{0,k}$ を適用することで、以下の基本差分方程式が得られる。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} = \sum_{k=0}^{r-1} \binom{r}{k} S_{0,k} \quad \dots (3) $$

### 3.2 一般漸化式の導出
式(3)の右辺から $k = r-1$ の項を分離する。

$$ \sum_{k=0}^{r-1} \binom{r}{k} S_{0,k} = r S_{0,r-1} + \sum_{k=0}^{r-2} \binom{r}{k} S_{0,k} $$

これを整理することで、以下の一般漸化式が導出される。

$$ S_{0,r-1} = \frac{1}{r} \left( \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} - \sum_{k=0}^{r-2} \binom{r}{k} S_{0,k} \right) \quad \dots (4) $$

## 4. 第2の柱：指数型母関数と連続的微分構造

本章では、数列 $\{S_{0,k}\}$ に対して指数型母関数を導入し、数列の対称性が偶関数の性質へ還元されるプロセスを示し、新たな偶数次基底 $g_{2m}^{(a)}$ を定義する。

### 4.1 指数型母関数 $F(t)$ の閉形式
数列 $\{S_{0,k}\}$ に対する指数型母関数 $F(t)$ を定義する。

$$ F(t) = \sum_{k=0}^{\infty} S_{0,k} \frac{t^k}{k!} = \sum_{j=1}^{n-1} j^a(n-j)^a e^{(n-j)t} $$

和の反転公式の対称性に基づき $j$ を $n-j$ に置換することで、等価な表現が得られる。

$$ F(t) = \sum_{j=1}^{n-1} j^a(n-j)^a e^{jt} \quad \dots (5) $$

### 4.2 関数方程式と偶関数 $G_a(t)$ への還元
式(5)から以下の関数方程式が成立する。

$$ F(t) = e^{nt} F(-t) $$

新たな関数 $G_a(t) = e^{-\frac{n}{2}t} F(t)$ を定義すると、$G_a(t) = G_a(-t)$ となり偶関数であることが証明される。指数関数は双曲線余弦関数 $\cosh$ へと還元される。

$$ G_a(t) = \sum_{j=1}^{n-1} j^a(n-j)^a \cosh\left( \left(\frac{n}{2}-j\right)t \right) \quad \dots (6) $$

### 4.3 偶数次基底 $g_{2m}^{(a)}$ の導入と統一的一般式
$G_a(t)$ のマクローリン展開における $t^{2m}/(2m)!$ の係数として、偶数次基底 $g_{2m}^{(a)} = G_a^{(2m)}(0)$ を定義する。

$$ g_{2m}^{(a)} = \sum_{j=1}^{n-1} j^a(n-j)^a \left(\frac{n}{2}-j\right)^{2m} \quad \dots (7) $$

奇数次導関数の消滅条件を適用することで、以下の統一的一般式が導出される。

$$ S_{0,k} = \sum_{m=0}^{\lfloor k/2 \rfloor} \binom{k}{2m} \left(\frac{n}{2}\right)^{k-2m} g_{2m}^{(a)} \quad \dots (8) $$

## 5. 第3の柱：代数的対称性に基づく恒等式と項の削減

本章では、恒等的にゼロとなる零空間を構築し、奇数次項を低次項へ還元する線形代数的操作を導出する。

### 5.1 奇数乗の対称性による恒等式
和のインデックス置換により、任意の $a, n, m$ において以下の恒等式が成立する。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a(n-2j)^{2m-1} = 0 \quad \dots (9) $$

### 5.2 奇数次項と偶数乗展開の線形削減公式
式(9)の左辺を二項展開し整理することで、$S_{0,k}$ 群の線形関係式が得られる。

$$ \sum_{k=0}^{2m-1} (-2)^k \binom{2m-1}{k} n^{2m-1-k} S_{0,k} = 0 \quad \dots (10) $$

また、偶数次基底 $g_{2m}^{(a)}$ を $S_{0,k}$ 系で直接記述する公式は以下の通りである。

$$ 2^{2m} g_{2m}^{(a)} = \sum_{k=0}^{2m} (-2)^k \binom{2m}{k} n^{2m-k} S_{0,k} \quad \dots (11) $$

## 6. パラメータ $a$ の階層間漸化式

### 6.1 微分方程式の導出
$G_a(t)$ の2階微分を計算し展開することで、パラメータ $a$ の階層を連結する微分方程式が確定する。

$$ G_{a+1}(t) = \frac{n^2}{4}G_a(t) - G_a''(t) \quad \dots (12) $$

### 6.2 離散基底の階層間漸化式
式(12)を微分し $t=0$ で評価することで、離散基底間の代数漸化式が得られる。

$$ g_{2m}^{(a+1)} = \frac{n^2}{4} g_{2m}^{(a)} - g_{2m+2}^{(a)} \quad \dots (13) $$

### 6.3 離散代数手法による階層間漸化式の別証
連続空間を経由せず、離散空間での純粋な代数操作から階層間漸化式が導出可能であることを証明する。一般化された畳み込み和 $X(a,b,c)$ を以下のように定義する。

$$ X(a,b,c) = \sum_{j=1}^{n-1} j^a(n-j)^b(n-2j)^c $$

パラメータ $a$ を単独で増加させる操作（被加数への $j$ の乗算）に対し、区間の中点 $\frac{n}{2}$ を用いた変数分解 $j = \frac{n}{2} - \frac{1}{2}(n-2j)$ を適用し、線形性により展開する。

$$ X(a+1, b, c) = \frac{n}{2} X(a, b, c) - \frac{1}{2} X(a, b, c+1) \quad \dots (14) $$

同様に、パラメータ $b$ を増加させる操作（被加数への $n-j$ の乗算）に対し、$n-j = \frac{n}{2} + \frac{1}{2}(n-2j)$ を適用する。

$$ X(a, b+1, c) = \frac{n}{2} X(a, b, c) + \frac{1}{2} X(a, b, c+1) \quad \dots (15) $$

式(14)および式(15)を組み合わせ、パラメータ $a, b$ の両方を増加させる操作（被加数への $j(n-j)$ の乗算）を評価する。これは $j(n-j) = \frac{n^2}{4} - \frac{1}{4}(n-2j)^2$ を乗じることと等価である。

$$ X(a+1, b+1, c) = \frac{n^2}{4} X(a, b, c) - \frac{1}{4} X(a, b, c+2) \quad \dots (16) $$

$a=b$ かつ $c=2m$ の条件下で $X(a,a,2m) = 2^{2m} g_{2m}^{(a)}$ を式(16)へ代入すると、連続的な微分構造に依存することなく式(13)の階層間漸化式が厳密に導出される。

## 7. 考察

### 7.1 パリティ・シフト作用としての非対称性
式(14)および式(15)における単一パラメータの遷移 $X(a+1,b,c)$ および $X(a,b+1,c)$ の展開は、総和空間における非対称性の役割を明確に示している。

パラメータの一方のみを増加させる操作は、次数が保存されるスカラー倍項（$\frac{n}{2} X(a,b,c)$）と、重み $(n-2j)$ の次数が $+1$ されるシフト項（$\pm\frac{1}{2} X(a,b,c+1)$）の線形結合を生成する。
基本反転公式 $X(a,b,c) = (-1)^c X(b,a,c)$ により、関数空間は $c$ の奇偶に応じて零空間（奇空間）と基底空間（偶空間）に二分される。したがって、式(14)や式(15)が示す次数 $c$ から $c+1$ へのシフトは、零空間の項を基底空間へ、あるいは基底空間の項を零空間へと相互に反転させる代数的な「パリティ・シフト作用」として機能する。

### 7.2 アルゴリズム計算量の削減
階層間漸化式（式13）は、総和記号を含まない純粋な代数演算である。これにより、パラメータ $a$ の基底配列から階層 $a+1$ の基底配列の生成が、反復和の $\mathcal{O}(n)$ から $\mathcal{O}(1)$ の計算量へと最適化される。

### 7.3 初期境界条件の確定
本理論の基点となる $a=0$ の階層の最小基底は $g_0^{(0)} = n-1$ であり、すべての高次構造はこの初期条件および既存のべき乗和公式を境界として決定論的に構築される。

## 8. 結論
本論文では、多項式差分における離散畳み込み和に対し、離散階差構造、連続的微分構造、代数的対称性の3つの独立したアプローチを統合した。これらのアプローチは偶数次基底 $g_{2m}^{(a)}$ に還元され、離散および連続の双方から同一の階層間漸化式が導出されることを証明した。本理論は、計算機代数における高次多項式生成の計算量を劇的に削減する計算基盤を提供する。

---

# 多項式差分における連鎖分割理論と畳み込みべき乗和の代数構造
**Theory of Linked Partitions in Polynomial Differences and Algebraic Structure of Convolutional Power Sums**

20260522
**著者**: 松田 正豊

## 概要
本論文では、二変数多項式の離散畳み込み和に対する統一的な代数構造を提示する。離散階差作用素による再帰的漸化式の導出、指数型母関数（EGF）を介した双曲線余弦関数 $\cosh$ への還元と微分方程式の構築、および和の順序反転に伴う対称性恒等式という3つの独立したアプローチを統合する。これにより、すべての奇数次項が偶数次基底へと代数的に還元されることを証明し、パラメータ $a$ の階層をまたぐ再帰的アルゴリズムを確立する。

---

## 1. 序論

### 1.1 背景
自然数のべき乗和 $\sum j^k$ の閉形式表現に関する研究は、ファウルハーバーの公式やベルヌーイ数、あるいは第2種スターリング数を用いた解析手法によって古典的に体系化されてきた。しかし、これらの既存理論は主に単一変数の固定されたべき乗に特化している。そのため、複数の変数が交錯する離散畳み込み和や、未定の次数パラメータを内包する一般化された多項式差分に対して、階層間を遷移する体系的な再帰構造を記述するには限界が存在する。

### 1.2 研究の目的とアプローチ
本研究の目的は、任意の非負整数パラメータ $a$ を保持した二変数多項式の離散畳み込み和 $S_{c,d} = \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d}$ を導入し、有限べき乗和の挙動を「多項式差分における連鎖分割理論」の一部として再定義することである。

この一般化された代数構造を解明するため、本論文では以下の3つの独立した数学的アプローチを展開し、それらを統合した単一の理論フレームワークを構築する。

1. **離散階差構造**: 階差の総和（テレスコーピング・サム）の原理を用い、パラメータ $a$ を固定した状態での任意の次数 $r$ における再帰的な代数漸化式を導出する。
2. **連続的微分構造**: 離散演算を指数型母関数（Exponential Generating Function, EGF）の空間へ写像し、双曲線余弦関数 $\cosh$ の微分特性を利用して偶数次基底 $g_{2m}^{(a)}$ を定義する。
3. **代数的対称性**: 和の順序反転に伴う対称性のみを根拠として恒等式（零空間）を構築し、奇数次項を低次の偶数次項へと還元する線形代数的操作を確立する。

### 1.3 本論文の構成
本論文の構成は以下の通りである。第2章において基礎定義および対称性の恒等式を定式化し、第3章で離散階差作用素による基本差分方程式を導出する。第4章では母関数空間への変換を行い、偶関数への還元プロセスから統一的な一般式を提示する。第5章では代数的対称性に基づく一般化された恒等式を展開し、奇数次項の消去プロセスを証明する。第6章では離散構造と微分構造を統合し、階層間漸化式を確立する。第7章で本理論の代数的意義とアルゴリズム上の特性を考察し、第8章で結論を述べる。

## 2. 基礎定義および対称性

本章では、後続の展開における理論的基盤となる離散畳み込み和を定義し、その演算過程において恒常的に成立する代数的対称性を定式化する。

### 2.1 畳み込みべき乗和の定義
任意の自然数 $n \ge 2$ と、非負整数のパラメータ $a$、および非負整数 $c, d$ に対して、二変数 $j$ および $n-j$ から構成される離散畳み込み和 $S_{c,d}$ を以下のように定義する。

$$ S_{c,d} = \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d} $$

ここで、$j^a(n-j)^a$ はすべての項に共通する基底要素であり、インデックス $c, d$ はこの基底に対するそれぞれの変数の追加的な次数を表す。本論文における目的は、特定の $n$ や $a$ の値に依存せず、この $S_{c,d}$ の集合が成す代数的な関係構造を明らかにすることである。特に $c=0$ の場合を $S_{0,k}$ と表記し、本理論における基本数列として扱う。

$$ S_{0,k} = \sum_{j=1}^{n-1} j^a(n-j)^{a+k} $$

### 2.2 和の順序反転に伴う対称性
有限和におけるインデックスの置換は、総和の値に影響を与えない。式(1)において、和のインデックス $j$ を $n-j$ に置換する操作を適用する。

$$ \sum_{j=1}^{n-1} j^{a+c}(n-j)^{a+d} = \sum_{n-j=1}^{n-1} (n-j)^{a+c} \{ n-(n-j) \}^{a+d} $$

和の範囲は $j=1$ から $n-1$ までのままであり、被加数は $(n-j)^{a+c}j^{a+d}$ となる。これは定義式における $c$ と $d$ を入れ替えたものに等しい。したがって、任意の非負整数 $a, c, d$ に対して以下の恒等式が成立する。

$$ S_{c,d} = S_{d,c} $$

この反転公式は、後続の展開において奇数次項と偶数次項のパリティを決定づけ、余剰な項を代数的に消去するための根底的な操作原理となる。

## 3. 第1の柱：離散階差構造による高次漸化式

本章では、離散階差作用素によるテレスコーピング・サム（階差の総和）の原理を応用し、任意の次数 $r$ において高次項 $S_{0,r-1}$ を低次項の線形結合として記述する基本漸化式を導出する。

### 3.1 テレスコーピング和と基本差分方程式
自然数 $r$ に対して、隣接する項の階差 $(j+1)^r - j^r$ を構成し、二項定理を用いて展開する。

$$ (j+1)^r - j^r = \sum_{k=0}^{r-1} \binom{r}{k} j^k $$

この階差式に対し、共通基底 $j^a(n-j)^a$ を乗じ、$j=1$ から $n-1$ まで総和をとる。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} = \sum_{j=1}^{n-1} j^a(n-j)^a \sum_{k=0}^{r-1} \binom{r}{k} j^k $$

右辺の総和の順序を入れ替え、二項係数をくくり出す。

$$ \sum_{k=0}^{r-1} \binom{r}{k} \sum_{j=1}^{n-1} j^{a+k}(n-j)^a $$

内側の総和は $S_{k,0}$ であり、対称性 $S_{k,0} = S_{0,k}$ を適用することで、以下の基本差分方程式が得られる。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} = \sum_{k=0}^{r-1} \binom{r}{k} S_{0,k} $$

### 3.2 一般漸化式の導出
式(4)の右辺から $k = r-1$ の項を分離する。

$$ \sum_{k=0}^{r-1} \binom{r}{k} S_{0,k} = r S_{0,r-1} + \sum_{k=0}^{r-2} \binom{r}{k} S_{0,k} $$

これを整理することで、以下の一般漸化式が導出される。

$$ S_{0,r-1} = \frac{1}{r} \left( \sum_{j=1}^{n-1} j^a(n-j)^a \{ (j+1)^r - j^r \} - \sum_{k=0}^{r-2} \binom{r}{k} S_{0,k} \right) $$

## 4. 第2の柱：指数型母関数と連続的微分構造

本章では、数列 $\{S_{0,k}\}$ に対して指数型母関数を導入し、数列の対称性が偶関数の性質へ還元されるプロセスを示し、新たな偶数次基底 $g_{2m}^{(a)}$ を定義する。

### 4.1 指数型母関数 $F(t)$ の閉形式
数列 $\{S_{0,k}\}$ に対する指数型母関数 $F(t)$ を定義する。

$$ F(t) = \sum_{k=0}^{\infty} S_{0,k} \frac{t^k}{k!} = \sum_{j=1}^{n-1} j^a(n-j)^a e^{(n-j)t} $$

和の反転公式の対称性に基づき $j$ を $n-j$ に置換することで、等価な表現が得られる。

$$ F(t) = \sum_{j=1}^{n-1} j^a(n-j)^a e^{jt} $$

### 4.2 関数方程式と偶関数 $G_a(t)$ への還元
式(6)から以下の関数方程式が成立する。

$$ F(t) = e^{nt} F(-t) $$

新たな関数 $G_a(t) = e^{-\frac{n}{2}t} F(t)$ を定義すると、$G_a(t) = G_a(-t)$ となり偶関数であることが証明される。指数関数は双曲線余弦関数 $\cosh$ へと還元される。

$$ G_a(t) = \sum_{j=1}^{n-1} j^a(n-j)^a \cosh\left( \left(\frac{n}{2}-j\right)t \right) $$

### 4.3 偶数次基底 $g_{2m}^{(a)}$ の導入と統一的一般式
$G_a(t)$ のマクローリン展開における $t^{2m}/(2m)!$ の係数として、偶数次基底 $g_{2m}^{(a)} = G_a^{(2m)}(0)$ を定義する。

$$ g_{2m}^{(a)} = \sum_{j=1}^{n-1} j^a(n-j)^a \left(\frac{n}{2}-j\right)^{2m} $$

奇数次導関数の消滅条件を適用することで、以下の統一的一般式が導出される。

$$ S_{0,k} = \sum_{m=0}^{\lfloor k/2 \rfloor} \binom{k}{2m} \left(\frac{n}{2}\right)^{k-2m} g_{2m}^{(a)} $$

## 5. 第3の柱：代数的対称性に基づく恒等式と項の削減

本章では、恒等的にゼロとなる零空間を構築し、奇数次項を低次項へ還元する線形代数的操作を導出する。

### 5.1 奇数乗の対称性による恒等式
和のインデックス置換により、任意の $a, n, m$ において以下の恒等式が成立する。

$$ \sum_{j=1}^{n-1} j^a(n-j)^a(n-2j)^{2m-1} = 0 $$

### 5.2 奇数次項と偶数乗展開の線形削減公式
式(10)の左辺を二項展開し整理することで、$S_{0,k}$ 群の線形関係式が得られる。

$$ \sum_{k=0}^{2m-1} (-2)^k \binom{2m-1}{k} n^{2m-1-k} S_{0,k} = 0 $$

また、偶数次基底 $g_{2m}^{(a)}$ を $S_{0,k}$ 系で直接記述する公式は以下の通りである。

$$ 2^{2m} g_{2m}^{(a)} = \sum_{k=0}^{2m} (-2)^k \binom{2m}{k} n^{2m-k} S_{0,k} $$

## 6. パラメータ $a$ の階層間漸化式

### 6.1 微分方程式の導出
$G_a(t)$ の2階微分を計算し展開することで、パラメータ $a$ の階層を連結する微分方程式が確定する。

$$ G_{a+1}(t) = \frac{n^2}{4}G_a(t) - G_a''(t) $$

### 6.2 離散基底の階層間漸化式
式(13)を微分し $t=0$ で評価することで、離散基底間の代数漸化式が得られる。

$$ g_{2m}^{(a+1)} = \frac{n^2}{4} g_{2m}^{(a)} - g_{2m+2}^{(a)} $$

### 6.3 離散代数手法による階層間漸化式の別証
一般化された畳み込み和 $X(a,b,c) = \sum j^a(n-j)^b(n-2j)^c$ を定義する。被加数に $j(n-j) = \frac{n^2}{4} - \frac{1}{4}(n-2j)^2$ を乗じ展開する。

$$ X(a+1, b+1, c) = \frac{n^2}{4} X(a, b, c) - \frac{1}{4} X(a, b, c+2) $$

$a=b$ かつ $c=2m$ の条件下で $X(a,a,2m) = 2^{2m} g_{2m}^{(a)}$ を代入すると、連続的な微分構造に依存することなく式(14)が厳密に導出される。

## 7. 考察

### 7.1 パリティ・シフト作用としての非対称性
関数 $X(a,b,c)$ におけるパラメータの非対称性（$a \neq b$）は、総和空間におけるパリティ・シフト作用素として機能する。変数 $j = \frac{n}{2} - \frac{1}{2}(n-2j)$ の乗算は、次数が保存されるスカラー倍項と、次数が $+1$ されるシフト項を生成する。この次元遷移により、非対称パラメータは零空間と基底空間を相互に反転させる代数演算として機能する。

### 7.2 アルゴリズム計算量の削減
階層間漸化式（式14）は、総和記号を含まない純粋な代数演算である。これにより、パラメータ $a$ の基底配列から階層 $a+1$ の基底配列の生成が、反復和の $\mathcal{O}(n)$ から $\mathcal{O}(1)$ の計算量へと最適化される。

### 7.3 初期境界条件の確定
本理論の基点となる $a=0$ の階層の最小基底は $g_0^{(0)} = n-1$ であり、すべての高次構造はこの初期条件および既存のべき乗和公式を境界として決定論的に構築される。

## 8. 結論
本論文では、多項式差分における離散畳み込み和に対し、離散階差構造、連続的微分構造、代数的対称性の3つの独立したアプローチを統合した。これらのアプローチは偶数次基底 $g_{2m}^{(a)}$ に還元され、離散および連続の双方から同一の階層間漸化式が導出されることを証明した。本理論は、計算機代数における高次多項式生成の計算量を劇的に削減する計算基盤を提供する。