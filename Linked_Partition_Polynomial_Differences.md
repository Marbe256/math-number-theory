# 多項式階差における連鎖分割理論：非対称和の純粋対称核への還元と一般化漸化式の導出
(Theory of Linked Partitions in Polynomial Differences: Reducing Asymmetric Sums and Deriving Generalized Recurrences)

日付：2026/03/04

著者：Masatoyo Masuda

## 概要 (Abstract)
多項式階差から生じる交差項を含む非対称な有限べき乗和において、次元の退化による不可逆性を回避し、純粋な対称核の線形結合へと還元する「連鎖分割理論（Theory of Linked Partitions）」を提唱する。本論文では、引き算のリュカ多項式の双対性から独自の還元公式を導出し、階差方程式における境界項の完全相殺メカニズムを解明した。結果として、計算量が $O(n)$ に依存する古典的な総和演算を、低次の純粋対称核群のみを再利用する $O(a)$ の自己完結的な一般化漸化式へと純化し、巨大な次元の離散和計算に対する極めて効率的な代数的アプローチを確立した。

---

## 1. 序論 (Introduction)

### 1.1 背景と動機 (Background and Motivation)
離散数学および数論において、連続する整数の有限べき乗和 $\sum j^c$ を閉じた形で表現する試みは、ファウルハーバーの公式やベルヌーイ多項式の発見以来、広く研究されてきた。これらの古典的アプローチは、単一の変数 $j$ に依存する多項式の和を扱う上で基本的な枠組みを提供する。

しかし、和の内部に変数 $j$ とその補数である $(n-j)$ が同時に介在し、それらが異なる次数で交差する非対称な畳み込み和 $\sum j^\alpha (n-j)^\beta$ （ただし $\alpha \neq \beta$）を扱う場合、既存の枠組みでは見通しが悪くなる。二項定理による展開と個別のべき乗和への分解を用いたトップダウン的な手法は、計算量の増大を招くだけでなく、和の背後に潜むインデックスの対称性という代数構造を捉えにくくするという課題を残していた。

### 1.2 連鎖分割理論の構想 (Concept of Linked Partition Theory)
本論文では、多項式階差に現れる交差項を含んだ有限べき乗和を、独立した項の集まりとしてではなく、次元間で連鎖する構造として捉え直す「連鎖分割理論（Theory of Linked Partitions）」を提唱する。

本理論は、変数 $j$ と $(n-j)$ が同次で均衡する「対称核（Symmetric Kernel）」を議論の基本単位とする。非対称な有限べき乗和を扱う際、単一変数の古典的な「有限べき乗和（次元ゼロへの退化）」に関する定理からアプローチすることはできない。次元の退化によって交差項の構造が失われるという、数学的な不可逆性が存在するためである。

この不可逆性を踏まえ、本論文では基準となる一般次元を保持し、独自の還元操作を用いることで、対象となる有限べき乗和を対称核の線形結合へと書き換えていく。そして、この一連の定式化の過程で、対称核の階差差分に関する一つの恒等式が見出されたことにより、本理論の実践的な有用性が裏付けられることとなった。

### 1.3 本論文の目的と主結果 (Objective and Main Results)
本論文の目的は、多項式階差の演算から生じる境界項（Boundary terms）の代数的な相殺メカニズムを示し、非対称和を対称核へ変換する還元公式を定式化することである。

結果として、次元 $n$ における対称核から次元 $n+1$ への前進差分を、系が持つ次元の重み係数と、低次元の対称核群のみを用いた自己完結的な漸化式として記述する。この一般化漸化式は、総和の計算量を $O(n)$ から基準次元に依存する再帰的な演算へと圧縮する手法を示しており、離散和における新たな計算論的視点を提供するものである。

---

## 2. 基礎定義と記号の省略表記 (Preliminaries and Abbreviated Notations)

本章では、連鎖分割理論を展開する上で基礎となる有限べき乗和の一般式を定義し、論文全体を通して使用する記号の省略表記、および前進差分を定式化する。

### 2.1 一般化された有限べき乗和 (Generalized Finite Power Sums)
任意の変数 $\alpha, \beta$ において、変数 $j$ と $(n-j)$ の交差項を含む一般化された有限べき乗和 $S(\alpha, \beta, n)$ を以下のように定義する。

$$
S(\alpha, \beta, n) = \sum_{j=1}^{n-1} j^\alpha (n-j)^\beta \quad \dots (1)
$$

この総和 $S(\alpha, \beta, n)$ は、 $\alpha, \beta$ が負の整数である場合や、上限を規定する $n$ が非整数、ゼロ、あるいは負の整数に拡張された場合においても有効に機能する普遍的な構造を持つが、本論文では離散的な多項式階差の代数構造を論じる目的から、非負整数および自然数 $n \ge 2$ の範囲を主眼に置いて展開する。

論文内での可読性を高め、次元間の相対的なシフトを明示するため、基準となる次元を $a$ とし、非対称な和に対する省略記法 $S_{c, d}$ を以下のように定義する。

$$
S_{c, d} \equiv S(a+c, a+d, n) \quad (c < d) \quad \dots (2)
$$

$$
S_{c-a, d-a} = S(c, d, n) \quad (c < d) \quad \dots (2')
$$


### 2.2 対称核の定義 (Definition of Symmetric Kernels)
式(1)において $\alpha = \beta$ となる場合、変数の次数が均衡し、総和は対称性を持つ。本理論ではこれを「対称核（Symmetric Kernel）」と呼ぶ。
対称核は本理論の基本基底となるため、引数 $n$ に依存する関数であることを前提としつつ、基準次元 $a$ に対する相対シフト $k$ を用いた省略記法 $S_k$ を以下のように定義する。

$$
S_k \equiv S(a+k, a+k, n) = \sum_{j=1}^{n-1} \{j(n-j)\}^{a+k} \quad \dots (3)
$$

$$
S_{k-a} = S(k, k, n) = \sum_{j=1}^{n-1} \{j(n-j)\}^{k} \quad \dots (3')
$$


この定義により、基準次元 $a$ そのものの対称核は $S_0$ として表現され、低次元（例えば次元 $k$）は相対的に $S_{k-a}(=S_k|_{a=0})  $ と表現される。


### 2.3 前進差分と階差の構造 (Forward Difference)
次元 $n$ における対称核から次元 $n+1$ への再帰的な代数構造を明らかにするため、相対シフト $k$ の対称核に対する $n$ についての前進差分 $\Delta S_k$ を定義する。本論文では主に基準次元の差分 $\Delta S_0$ を扱う。

$$
\Delta S_0 = S_0(n+1) - S_0(n) \quad \dots (4)
$$

この定義を出発点とし、次章において意図的に非対称性を与えた式から階差方程式を構築していく。

---

## 3. 対称性の分岐と境界項の相殺 (Bifurcation of Symmetry and Cancellation of Boundary Terms)

本章では、意図的に非対称性を与えた有限べき乗和の階差方程式を出発点とし、基準対称核の階差 $\Delta S_0$ を導出する。その過程で生じる境界項が代数的に相殺され、次元間の再帰的な関係式が構築されるメカニズムを示す。

### 3.1 非対称和からの階差方程式の構築
基準次元 $a$ に対し、次数に $1$ の次数の差を与えた非対称和 $S_{0, 1}$ を考える。次元 $n+1$ における和に対し、括弧内の項を $(n-j) + 1$ と分割して二項展開を行うアプローチと、インデックスの対称性を利用して変数構造を反転させるアプローチの2つの視点を適用する。これらの展開と整理を行うことで、対称核の差分 $\Delta S_0$ と低次の非対称和 $S_{k-a, 0}$ を含む2つの独立した漸化式が得られる。

### 3.2 次元の重みと境界項の抽出
得られた2つの漸化式を統合し、 $\Delta S_0$ について整理する。この代数的操作の過程で、系のトータル次元 $2a+1$ が、対称核 $S_0$ の係数として自然に抽出される。同時に、和のインデックスの上限が拡張されることに起因して、境界項 $n^a(n-1)$ が方程式の左辺に出現する。

$$
n \Delta S_0 - n^a(n-1) = (2a + 1) S_0 + \Delta S_0 + 2 \sum_{k=1}^{a-1} \binom{a}{k-1} S_{k-a, 0} - \sum_{k=1}^{a-1} \binom{a}{k-1} n^k
$$

### 3.3 境界項の相殺と基礎漸化式の導出
上式の右辺に含まれる $n^k$ の総和項は、二項定理の微分形を用いることで代数的に再構成でき、正確に $n^a(n-1) - \Delta S_0$ に一致することが示される。この結果を元の方程式に代入すると、左辺の境界項は右辺から生じた同項と相殺される。境界項が消滅した結果、次元 $n$ と $n+1$ の対称核を結ぶ、以下の基礎漸化式が得られる詳細な証明プロセスは付録Aを参照のこと）。

$$
(n - 1) \Delta S_0 = (2a + 1) S_0 + 2 \sum_{k=1}^{a-1} \binom{a}{k-1} S_{k-a, 0} \quad \dots (5)
$$

この恒等式は、基準対称核の階差 $\Delta S_0$ が、自身の次元の重みを持つ対称核 $S_0$ と、より低次の非対称和 $S_{k-a, 0}$ の線形結合によって記述されることを示している。

---

## 4. 非対称成分の完全還元定理 (Complete Reduction Theorem of Asymmetric Components)

前章で導出された基礎漸化式（式(5)）の右辺には、依然として非対称和が残存している。本章では、系の対称性を利用してこの非対称和を解体し、純粋な対称核のみで記述される線形結合へと還元する定理を定式化する。

### 4.1 対称化と「引き算のリュカ多項式」の双対性
任意の非負整数 $c$ に対し、非対称和 $S_{0, c}$ のインデックスを反転させて自身の和をとることで、以下のように対称化された形に変形できる。

$$
S_{0, c} = \frac{1}{2} \sum_{j=1}^{n-1} \{ j(n-j) \}^a \{ j^c + (n-j)^c \}
$$

上式の後半に現れる $j^c + (n-j)^c$ を展開して基底に吸収させる必要がある。ここで留意すべきは、交差項の退化に伴う「不可逆性」が存在するため、既存の単一変数用の対称式定理を適用することは原理的に不可能であるという点である。
この不可逆な壁を突破するため、本論文では「引き算のリュカ多項式（Subtraction Lucas polynomials）」が持つ係数構造に着目した。和 $n$ と積 $j(n-j)$ を変数とする系の双対性を観察することにより、既存の定理に依存せず、この交差構造に適合する独自の展開係数を導出した。

### 4.2 還元公式の定式化
引き算のリュカ多項式の双対性から導かれた独自の係数構造を適用し、共通項 $\{ j(n-j) \}$ の次数を整理することで、非対称和 $S_{0, c}$ は以下の一般項として普遍的に成立する。

$$
S_{0, c} = \frac{1}{2} \sum_{k=0}^{\lfloor c/2 \rfloor} \mathcal{A}_{c,k} \cdot n^{c-2k} \cdot S_k \quad \dots (6)
$$

ここで、展開係数 $\mathcal{A}_{c,k}$ は、次数の差 $c$ とインデックス $k$ に依存する以下の閉じた式で与えられる（ $c \ge 1$）。

$$
\mathcal{A}_{c,k} = (-1)^k \frac{c}{c-k} \binom{c-k}{k} \quad \dots (7)
$$

式(6)および式(7)は、次数の差 $c$ を持つ非対称和が、 $n$ の多項式を係数として、基準から相対的に $+k$ シフトした純粋な対称核 $S_k$ の線形結合へと翻訳されることを示している。

### 4.3 次元退化の不可逆性と本定理の独自性
式(6)において基準次元 $a=0$ とした場合、左辺は単一変数の有限べき乗和となり、右辺はそれを表現する既知の形と一致する。しかし、この $a=0$ の状態から出発して式(6)を導出することはできない。変数が単一化された次元へ退化すると元の系の構造が失われるためである。したがって、式(6)の還元公式は古典的な公式の流用ではなく、不可逆性を前提とした上で双対性から独自に到達した、多項式階差に固有の普遍的な代数構造である。

---

## 5. 主定理：完全純化された一般化漸化式 (Main Theorem: Completely Purified Generalized Recurrence Relation)

前章で定式化された還元公式（式(6), 式(7)）を用いることで、基礎漸化式に残存していた非対称和を純粋な対称核のみを基底とする系へと変換することが可能となる。本章では、多項式階差における一般化漸化式を主定理として定式化する。

ここで、表記の統一のため基礎漸化式（式(5)）を再掲する。

$$
(n - 1) \Delta S_0 = (2a + 1) S_0 + 2 \sum_{k=1}^{a-1} \binom{a}{k-1} S_{k-a, 0} \quad \dots (5)
$$

### 5.1 多項式係数 $C_k(n)$ の定義
式(5)の右辺における非対称和 $S_{k-a, 0}$ に対して還元公式を適用し、インデックスを再編して純粋対称核の絶対次元 $k$ （相対表記 $S_{k-a}$）について整理する（詳細な代数的プロセスについては付録Bを参照のこと）。この操作により、各低次対称核に掛かる $n$ の多項式係数 $C_k(n)$ が以下のように一意に定まる。

$$
C_k(n) = \frac{1}{a-k} \sum_{i=\max(1, 2k-a)}^{k} (-1)^{k-i} (a-i) \binom{a}{i-1} \binom{a-k}{k-i} n^{a-2k+i} \quad \dots (8)
$$

### 5.2 主定理の定式化
係数 $C_k(n)$ を用いることで、式(5)から非対称な要素が排除され、本論文の主定理である以下の恒等式が得られる。

**【主定理】（完全純化された一般化漸化式）**
任意の自然数 $a \ge 1, n \ge 2$ において、基準次元 $a$ の対称核の前進差分 $\Delta S_0$ は以下の自己完結的な漸化式を満たす。

$$
(n - 1) \Delta S_0 = (2a + 1) S_0 + \sum_{k=1}^{a-1} C_k(n) S_{k-a} \quad \dots (9)
$$

（※ただし、 $a=1$ のとき右辺の和は空和となり $0$ として扱う。）

この主定理は、基準次元における対称核の階差が、トータル次元に由来する重み $(2a+1)$ を持った自身の対称核 $S_0$ と、より低次の純粋な対称核群 $S_{k-a}$ の多項式係数による線形結合のみで記述されることを示している。

---

## 6. 理論の実証と計算論的有用性 (Demonstrations and Computational Utility)

主定理（式(9)）の定式化により、巨大な $n$ を持つ複雑な有限べき乗和の計算は、インデックス $j$ に依存する総和演算から解放される。定義に従えば $O(n)$ の計算量を要求する有限和が、過去の次元の対称核を再利用する再帰的演算へと劇的に圧縮されるのである。

この一般化漸化式が強力な「生成エンジン」として機能し、複雑なシグマ計算を経由せずに次々と高次の対称核の値を導き出す具体的なプロセスと検算については、付録Cにて基準次元 $a=1, 2, 3$ のケースを用いて実証する。

---

## 7. 結論と今後の展望 (Conclusion and Future Work)

本論文では、「連鎖分割理論（Linked Partition Theory）」という新たな枠組みを提唱し、多項式階差に現れる非対称な有限べき乗和を扱うための普遍的な代数構造を明らかにした。

既存の有限べき乗和の定理への退化がもたらす不可逆性を回避するため、本理論は一般次元を保持し、引き算のリュカ多項式の双対性から独自の還元公式を導出した。その結果、系に生じる境界項の完全相殺メカニズムを解明し、一切の非対称ノイズを排除した自己完結的な一般化漸化式（式(9)）の構築に成功した。

---

## 付録A：基礎漸化式における境界項の相殺プロセスの証明

本付録では、第3章で提示された基礎漸化式（式5）の導出過程において、非対称和の展開から生じた境界項がどのようにして相殺されるのか、その詳細な代数的プロセスを証明する。

### A.1 非対称和 $S_{0,1}$ の2つの評価アプローチ
次数に $1$ の次数の差を与えた非対称和 $S_{0, 1}(n) = \sum_{j=1}^{n-1} j^a (n-j)^{a+1}$ を考える。和のインデックスの対称性（ $j \to n-j$）を利用すると、この和は対称核 $S_0(n)$ を用いて次のように評価できる。

$$
S_{0, 1}(n) = \frac{n}{2} S_0(n) \quad \dots (A.1)
$$

次元を $n+1$ に引き上げた $S_{0,1}(n+1)$ について、2つの異なるアプローチで展開を行う。
第一のアプローチとして、式(A.1)の定義に従ってそのまま次元を上げる。

$$
S_{0, 1}(n+1) = \frac{n+1}{2} S_0(n+1) = \frac{n+1}{2} \{ S_0(n) + \Delta S_0 \} \quad \dots (A.2)
$$

第二のアプローチとして、 $S_{0, 1}(n+1)$ のシグマ定義式に戻り、括弧内を $(n-j)+1$ と分割して二項展開を行う。

$$
S_{0, 1}(n+1) = \sum_{j=1}^n j^a \{ (n-j) + 1 \}^{a+1} = \sum_{j=1}^n j^a \sum_{m=0}^{a+1} \binom{a+1}{m} (n-j)^m \quad \dots (A.3)
$$

式(A.3)の和の上限 $j=n$ においては、 $m=0$ の項のみが生き残り、境界項 $n^a$ を生出する。これを取り出し、残りの和を $S$ の記法で整理する。

$$
S_{0, 1}(n+1) = n^a + S_{0, 1}(n) + (a+1)S_0(n) + \sum_{m=0}^{a-1} \binom{a+1}{m} S_{m-a, 0}(n) \quad \dots (A.4)
$$

### A.2 次元拡張の方程式と $\Delta S_0$ の抽出
式(A.2)と式(A.4)は等価である。式(A.4)の $S_{0, 1}(n)$ に式(A.1)を代入して両辺を等置し、両辺を $2$ 倍して整理する。

$$
(n+1)S_0(n) + (n+1)\Delta S_0 = 2n^a + n S_0(n) + 2(a+1)S_0(n) + 2\sum_{m=0}^{a-1} \binom{a+1}{m} S_{m-a, 0}(n) \quad \dots (A.5)
$$

両辺から $(n+1)S_0(n)$ を減算することで、階差 $\Delta S_0$ に関する第一の方程式が得られる。

$$
(n+1)\Delta S_0 = 2n^a + (2a+1)S_0 + 2\sum_{m=0}^{a-1} \binom{a+1}{m} S_{m-a, 0} \quad \dots (A.6)
$$

### A.3 対称核の前進差分に基づく相殺
式(A.6)には依然として境界項 $2n^a$ が残存している。これを相殺するため、純粋な対称核の前進差分 $\Delta S_0$ そのものを二項展開から直接評価する。

$$
\Delta S_0 = S_0(n+1) - S_0(n) = \sum_{j=1}^n j^a \{ (n-j) + 1 \}^a - \sum_{j=1}^{n-1} j^a(n-j)^a \quad \dots (A.7)
$$

式(A.3)と同様に展開し、 $j=n$ の境界項を抽出すると、次の恒等式が得られる。

$$
\Delta S_0 = n^a + \sum_{m=0}^{a-1} \binom{a}{m} S_{m-a, 0} \quad \dots (A.8)
$$

両辺を $2$ 倍する。

$$
2\Delta S_0 = 2n^a + 2\sum_{m=0}^{a-1} \binom{a}{m} S_{m-a, 0} \quad \dots (A.9)
$$

最後に、式(A.6)から式(A.9)を辺々減算する。この操作により、厄介な境界項 $2n^a$ は相殺され消滅する。

$$
(n-1)\Delta S_0 = (2a+1)S_0 + 2\sum_{m=0}^{a-1} \biggl( \binom{a+1}{m} - \binom{a}{m} \biggr) S_{m-a, 0} \quad \dots (A.10)
$$

パスカルの法則 $\binom{a+1}{m} - \binom{a}{m} = \binom{a}{m-1}$ を適用する。（※ $m=0$ のとき $\binom{a}{-1} = 0$ となり初項も消滅する。）
インデックスを $k = m$ と置き換えることで、本文の第3章で示した基礎漸化式（式5）が証明される。

$$
(n-1)\Delta S_0 = (2a+1)S_0 + 2\sum_{k=1}^{a-1} \binom{a}{k-1} S_{k-a, 0} \quad \dots (A.11)
$$

---

## 付録B：非対称項の解体と再構築プロセスの詳細

本付録では、主定理の多項式係数 $C_k(n)$ が、基礎漸化式の右辺に残存する非対称和群からどのように抽出され、再構築されるかの代数的プロセスを詳述する。

### B.1 還元公式の適用と二重和の形成
基礎漸化式（式(5) / 式(A.11)）の右辺に含まれる非対称項の和を $\Sigma_{\text{asym}}$ と置く。和のインデックスを $i$ とし、還元公式（式(6)）における一般相対シフトの性質  $S_{0, c} = \frac{1}{2}\sum \mathcal{A}_{c,j}n^{c-2j}S_j$ を用いて  $S_{i-a, 0}$（すなわち $S_{0, a-i}$ と等価）を展開すると、以下の二重和が得られる。

$$
\Sigma_{\text{asym}} = 2 \sum_{i=1}^{a-1} \binom{a}{i-1} \biggl( \frac{1}{2} \sum_{j=0}^{\lfloor (a-i)/2 \rfloor} \mathcal{A}_{a-i, j} \cdot n^{a-i-2j} \cdot S_{i-a+j} \biggr) \quad \dots (B.1)
$$

係数 $2$ と $\frac{1}{2}$ が相殺される。

$$
\Sigma_{\text{asym}} = \sum_{i=1}^{a-1} \binom{a}{i-1} \sum_{j=0}^{\lfloor (a-i)/2 \rfloor} \mathcal{A}_{a-i, j} \cdot n^{a-i-2j} \cdot S_{i-a+j} \quad \dots (B.2)
$$

### B.2 主項と交差補正項の分離 ( $j=0$ の抽出)
本理論における引き算のリュカ多項式の役割を明確にするため、内側の和から $j=0$ の項を分離する。式(7)より $\mathcal{A}_{c, 0} = 1$ であることを踏まえ、式(B.2)を2つのパートに分割する。

$$
\Sigma_{\text{asym}} = \underbrace{ \sum_{i=1}^{a-1} \binom{a}{i-1} n^{a-i} S_{i-a} }_{\text{主項}} + \underbrace{ \sum_{i=1}^{a-1} \binom{a}{i-1} \sum_{j=1}^{\lfloor (a-i)/2 \rfloor} \mathcal{A}_{a-i, j} \cdot n^{a-i-2j} \cdot S_{i-a+j} }_{\text{交差補正項}} \quad \dots (B.3)
$$

前半の「主項」は、交差項の干渉を受けない直接的な次元マッピングを表す。一方、後半の「交差補正項」は、係数 $\mathcal{A}_{a-i, j}$ の介入によって交差構造による次元の歪みを精緻に補正し、より高い次元へと分配する役割を担っている。

### B.3 同次対称核の集約と $C_k(n)$ の導出
分離した項を再び統合し、絶対次元を示すインデックス $k = i + j$ （すなわち相対表記 $S_{k-a}$）について系全体を再編成する。
$j = k - i$ を代入して和の順序を入れ替え、各次元の対称核 $S_{k-a}$ を括り出すと次のように整理される。ここで、和の範囲は $1 \le k-i \le \lfloor (a-i)/2 \rfloor$ の制約から $i \ge \max(1, 2k-a)$ と定まる。

$$
\Sigma_{\text{asym}} = \sum_{k=1}^{a-1} \biggl( \sum_{i=\max(1, 2k-a)}^{k} \binom{a}{i-1} \mathcal{A}_{a-i, k-i} \cdot n^{a-2k+i} \biggr) S_{k-a} \quad \dots (B.4)
$$

括弧内の係数群に対し、式(7)の定義式 $\mathcal{A}_{a-i, k-i} = (-1)^{k-i} \frac{a-i}{a-k} \binom{a-k}{k-i}$ を代入する。

$$
C_k(n) = \sum_{i=\max(1, 2k-a)}^{k} \binom{a}{i-1} \biggl( (-1)^{k-i} \frac{a-i}{a-k} \binom{a-k}{k-i} \biggr) n^{a-2k+i} \quad \dots (B.5)
$$

これを代数的に整理することで、本文の式(8)である明示公式が導出される。

$$
C_k(n) = \frac{1}{a-k} \sum_{i=\max(1, 2k-a)}^{k} (-1)^{k-i} (a-i) \binom{a}{i-1} \binom{a-k}{k-i} n^{a-2k+i} \quad \dots (B.6)
$$

---

## 付録C：完全純化エンジンによる対称核の自動生成と検算

本付録では、主定理（式9）から導出される具体的な漸化式を示し、シグマ計算（定義）による直接計算との一致を検証する。基準次元 $a$ に対して、相対表記 $S_0, S_{-1}, S_{-2}$ 等を用いて次元の遷移を明示する。
なお、すべての次元において初期値は $S_k(2) = \sum_{j=1}^1 \{j(2-j)\}^{a+k} = 1$ である。

### C.1 $a=1$ のケース：純粋な比率による生成
式(9)において基準次元 $a=1$ とすると、右辺のシグマは空和となり、過去の次元に依存しない以下の基礎エンジンが得られる。

**【漸化式】**
$$(n - 1) S_0(n+1) = (n + 2) S_0(n)$$

**【ステップ実行と検算】**
* **$n=2 \to 3$ の生成:**
  * [漸化式] $(2 - 1) S_0(3) = (2 + 2) \cdot S_0(2) \implies 1 \cdot S_0(3) = 4 \cdot 1 \implies \mathbf{S_0(3) = 4}$
  * [検　算] $S_0(3) = \sum_{j=1}^2 \{j(3-j)\}^1 = (1\cdot 2) + (2\cdot 1) = 2 + 2 = \mathbf{4}$ （一致）

* **$n=3 \to 4$ の生成:**
  * [漸化式] $(3 - 1) S_0(4) = (3 + 2) \cdot S_0(3) \implies 2 \cdot S_0(4) = 5 \cdot 4 \implies \mathbf{S_0(4) = 10}$
  * [検　算] $S_0(4) = \sum_{j=1}^3 \{j(4-j)\}^1 = (1\cdot 3) + (2\cdot 2) + (3\cdot 1) = 3 + 4 + 3 = \mathbf{10}$ （一致）


### C.2 $a=2$ のケース：低次元核の組み込み
式(9)において基準次元 $a=2$ のとき、 $C_1(n) = n$ となる。相対表記 $S_{-1}$ は絶対次元 $1$ の対称核（C.1で求めた値）を指す。

**【漸化式】**
$$(n - 1) S_0(n+1) = (n + 4) S_0(n) + n S_{-1}(n)$$
*(※事前準備：低次元核の値  $S_{-1}(2)=1,\ S_{-1}(3)=4,\ S_{-1}(4)=10$)*

**【ステップ実行と検算】**
* **$n=2 \to 3$ の生成:**
  * [漸化式] $(2 - 1) S_0(3) = (2 + 4) S_0(2) + 2 \cdot S_{-1}(2) \implies 1 \cdot S_0(3) = 6 \cdot 1 + 2 \cdot 1 \implies \mathbf{S_0(3) = 8}$
  * [検　算] $S_0(3) = \sum_{j=1}^2 \{j(3-j)\}^2 = (1\cdot 2)^2 + (2\cdot 1)^2 = 4 + 4 = \mathbf{8}$ （一致）

* **$n=3 \to 4$ の生成:**
  * [漸化式] $(3 - 1) S_0(4) = (3 + 4) S_0(3) + 3 \cdot S_{-1}(3) \implies 2 \cdot S_0(4) = 7 \cdot 8 + 3 \cdot 4 = 68 \implies \mathbf{S_0(4) = 34}$
  * [検　算] $S_0(4) = \sum_{j=1}^3 \{j(4-j)\}^2 = (1\cdot 3)^2 + (2\cdot 2)^2 + (3\cdot 1)^2 = 9 + 16 + 9 = \mathbf{34}$ （一致）


### C.3 $a=3$ のケース：複数次元の連鎖構造
式(9)において基準次元 $a=3$ のとき、 $C_1(n) = n^2, C_2(n) = 3n - 2$ となる。 $S_{-1}, S_{-2}$ はそれぞれ絶対次元 $2, 1$ の対称核を指す。

**【漸化式】**
$$(n - 1) S_0(n+1) = (n + 6) S_0(n) + (3n - 2) S_{-1}(n) + n^2 S_{-2}(n)$$
*(※事前準備：低次元核の値  $S_{-1}(2)=1,\ S_{-1}(3)=8$ および $S_{-2}(2)=1,\ S_{-2}(3)=4$)*

**【ステップ実行と検算】**
* **$n=2 \to 3$ の生成:**
  * [漸化式] $(2 - 1) S_0(3) = (2 + 6) S_0(2) + (6 - 2) S_{-1}(2) + 2^2 S_{-2}(2) \implies 1 \cdot S_0(3) = 8 \cdot 1 + 4 \cdot 1 + 4 \cdot 1 \implies \mathbf{S_0(3) = 16}$
  * [検　算] $S_0(3) = \sum_{j=1}^2 \{j(3-j)\}^3 = (1\cdot 2)^3 + (2\cdot 1)^3 = 8 + 8 = \mathbf{16}$ （一致）

* **$n=3 \to 4$ の生成:**
  * [漸化式] $(3 - 1) S_0(4) = (3 + 6) S_0(3) + (9 - 2) S_{-1}(3) + 3^2 S_{-2}(3) \implies 2 \cdot S_0(4) = 9 \cdot 16 + 7 \cdot 8 + 9 \cdot 4 = 144 + 56 + 36 = 236 \implies \mathbf{S_0(4) = 118}$
  * [検　算] $S_0(4) = \sum_{j=1}^3 \{j(4-j)\}^3 = (1\cdot 3)^3 + (2\cdot 2)^3 + (3\cdot 1)^3 = 27 + 64 + 27 = \mathbf{118}$ （一致）

