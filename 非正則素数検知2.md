# 多項式差分の連鎖分割と双曲線関数の対応に基づく非正則素数の評価手法と計算量 $O(p \log p)$ アルゴリズムの提案
(An Evaluation Method for Irregular Primes Based on the Correspondence Between Linked Partitions of Polynomial Differences and Hyperbolic Functions, and Proposal of an $O(p \log p)$ Algorithm)

- 2026/5/6 
- Masatoyo Matsuda

## 概要 (Abstract)
本論文では、有理数演算を回避し、多項式差分の連鎖分割から導出される漸化式を用いた非正則素数の評価手法を提案する。有限べき乗和の係数を決定する漸化式が、双曲線関数のマクローリン展開における恒等式と対応することを示し、これを形式的べき級数の畳み込みとして定式化する。さらに、任意模数数論変換（NTT）とモジュロ空間の次元降下を適用することで、特異点における計算不能状態を回避しつつ、時間計算量 $O(p \log p)$ で非正則素数および高次 $p$-進展開係数を抽出するアルゴリズムを構築する。従来手法が有理数演算の制約や $O(p^2)$ の時間計算量に依存するのに対し、本手法は理論面で有理数を排除する代数構造を持ち、アルゴリズム面で計算量を対数線形へと削減する点で優位性を持つ。

## 第1章：序論 (Chapter 1: Introduction)

非正則素数（irregular primes）の分布や性質の解明は、代数的整数論において継続的な研究対象である。素数 $p$ が非正則であるか否かの判定は、ベルヌーイ数 $B_k$ の分子が $p$ で割り切れるかどうかに依存する。しかし、ベルヌーイ数は有理数であり、フォン・シュタウト＝クラウゼンの定理（von Staudt-Clausen theorem）により、特定のインデックスにおいてその分母は $p$ を素因数として含む。この性質は、計算機上で高次の剰余環 $\mathbb{Z}/p^n\mathbb{Z}$ を用いてベルヌーイ数を直接評価する際の制約となる。

従来の大規模な非正則素数の探索としては、Hart, Harvey, Ong (2017) により $p < 2^{31}$ までの計算が達成されている。彼らの手法は時間計算量 $O(p \log^2 p)$ であり効率的であるが、その評価空間は第一次の非正則性（$B_k \equiv 0 \pmod p$）に限定されている。一方、超非正則素数（$B_k \equiv 0 \pmod{p^2}$）やさらに高次の $p$-進展開係数に関する計算は、Kellner (2007) による研究が存在するものの、有理数演算の計算機的制約からその評価範囲は $p < 1000$ に留まっているのが現状である。

本研究の目的は、多項式差分の連鎖分割から得られる漸化式の解析的構造を解明し、それに基づく時間計算量 $O(p \log p)$ の評価アルゴリズムを提案することである。本手法は従来法に対し、理論およびアルゴリズムの双方で以下の優位性を持つ。

理論面においては、離散的な多項式係数の漸化式が連続的な双曲線関数の恒等式に対応することを示し、漸化式の計算を形式的べき級数（FPS）の畳み込みへ帰着させる。この代数構造への写像により、フォン・シュタウト＝クラウゼンの特異点に起因する有理数演算を回避する。

アルゴリズム面においては、この理論的対応を基盤とし、任意模数数論変換（Arbitrary Modulo NTT）およびニュートン法による逆元計算を導入することで、係数評価の計算量を従来手法の $O(p \log^2 p)$ から $O(p \log p)$ へと削減する。さらに、モジュロ空間の次元降下（dimensional descent）機構を設けることで、ウィーフェリッヒ素数などの特異点においても偽陽性を数論的に排除し、Kellnerの探索限界を超える巨大素数の高次モジュロ空間において、決定論的に $p$-進展開係数を抽出可能とする。

## 第2章：連鎖分割漸化式の解析的構造 (Chapter 2: Analytic Structure of Linked Partition Recurrences)

本章では、多項式差分の連鎖分割から導出される有限べき乗和の基底係数に関する漸化式を定義し、その計算構造が形式的べき級数の畳み込み、および連続的な双曲線関数の恒等式と同型であることを示す。

### 2.1 連鎖分割漸化式と形式的べき級数への変換

本手法の基礎となる多項式差分の連鎖分割理論では、まず一般化された有限べき乗和を以下のように定義する。

$$ S(a, b, n) = \sum_{j=1}^{n-1} j^a (n-j)^b \tag{1} $$

本理論では、この式における奇数次の非対称な項（差分 $c$）を、偶数次の非対称性を持つ基底項（差分 $2m$）の線形結合として次のように展開する。

$$ L_c S(a, a+c, n) = \sum_{m=0}^{\frac{c-1}{2}} R_{2m} n^{c-2m} S(a, a+2m, n) \tag{2} $$

この多項式展開において、各基底項の重みを決定する基底係数 $R_{2m}$ は、対象とする多項式の反対称性条件から、ベース次数 $a$ に依存しない以下の漸化式によって一意に決定される。

$$ R_{2m} = \frac{1}{2} \left( L_c \binom{c}{2m} - \sum_{j=m+1}^{\frac{c-1}{2}} R_{2j} \binom{2j}{2m} \right) \tag{3} $$

ここで $c$ は対象とする最大の次数（素数評価においては $c = p-2$）、$L_c$ は定数である。この漸化式は、高次の係数 $R_{2j}$ から低次の係数 $R_{2m}$ を逐次的に求める構造を持つ。

式(3)における二項係数を階乗形式に展開し、両辺に $(2m)!$ を乗じることで、階乗成分を相殺し以下の式を得る。

$$ R_{2m}(2m)! = \frac{1}{2} \left( L_c \frac{c!}{(c-2m)!} - \sum_{j=m+1}^{\frac{c-1}{2}} R_{2j} \frac{(2j)!}{(2j-2m)!} \right) \tag{4} $$

式(4)の右辺における独立項は $j=m$ の場合の総和項と一致する。これを総和記号の内部に吸収し、インデックスを降順から昇順へ反転させる。$N = \frac{c-1}{2}$ とし、新たなインデックス $M = N - m$ および $K = N - j$ を導入する。さらに、以下の3つの数列 $\tilde{A}_K$, $\tilde{B}_k$, $\tilde{C}_M$ を定義する。

$$ \tilde{A}_K = R_{c-2K-1} \cdot (c-2K-1)! \tag{5} $$
$$ \tilde{B}_k = \frac{1}{(2k)!} \quad (\text{ただし } \tilde{B}_0 = 2) \tag{6} $$
$$ \tilde{C}_M = L_c \frac{c!}{(2M+1)!} \tag{7} $$

これらの置換を式(4)へ適用することで、元の漸化式は以下の離散畳み込み方程式へと変換される。

$$ \sum_{K=0}^{M} \tilde{A}_K \cdot \tilde{B}_{M-K} = \tilde{C}_M \tag{8} $$

式(8)は、未知の係数配列 $\tilde{A}$ と既知の配列 $\tilde{B}$ の畳み込みが既知の配列 $\tilde{C}$ に等しいことを示している。これは形式的べき級数 $\tilde{A}(x)$, $\tilde{B}(x)$, $\tilde{C}(x)$ を用いて、多項式の乗算 $\tilde{A}(x) \tilde{B}(x) = \tilde{C}(x)$ として記述される。

### 2.2 双曲線関数の恒等式への写像

前節で定義された形式的べき級数 $\tilde{B}(x)$ および $\tilde{C}(x)$ の解析的性質を評価するため、$x = t^2$ なる変数変換を行い、マクローリン展開の母関数として定式化する。

配列 $\tilde{B}_k$ を係数とする級数 $\tilde{B}(t^2)$ は、定数項 $\tilde{B}_0 = 2$ を除き、偶数次の階乗の逆数で構成される。これは双曲線余弦関数 $\cosh(t)$ のテイラー級数展開と以下の関係を持つ。

$$ \tilde{B}(t^2) = 1 + \sum_{k=0}^\infty \frac{t^{2k}}{(2k)!} = 1 + \cosh(t) \tag{9} $$

同様に、配列 $\tilde{C}_M$ を係数とする級数 $\tilde{C}(t^2)$ は、奇数次の階乗の逆数で構成され、双曲線正弦関数 $\sinh(t)$ を用いて以下のように表される。

$$ \tilde{C}(t^2) = L_c c! \sum_{M=0}^\infty \frac{t^{2M}}{(2M+1)!} = \frac{L_c c!}{t} \sinh(t) \tag{10} $$

求める係数配列の母関数 $\tilde{A}(t^2)$ は、代数関係 $\tilde{A}(x) = \tilde{C}(x) \tilde{B}^{-1}(x)$ より、式(9)および式(10)を用いて次のように導出される。

$$ \tilde{A}(t^2) = \frac{L_c c!}{t} \left( \frac{\sinh(t)}{1 + \cosh(t)} \right) \tag{11} $$

ここで、双曲線関数の半角の公式を適用する。$\sinh(t) = 2 \sinh(t/2) \cosh(t/2)$ および $1 + \cosh(t) = 2 \cosh^2(t/2)$ であるから、式(11)の括弧内の項は以下のように簡約化される。

$$ \frac{\sinh(t)}{1 + \cosh(t)} = \frac{2 \sinh(t/2) \cosh(t/2)}{2 \cosh^2(t/2)} = \tanh(t/2) \tag{12} $$

以上より、未知の係数配列 $\tilde{A}_K$ の母関数は以下の解析的表現に帰着する。

$$ \tilde{A}(t^2) = \frac{L_c c!}{t} \tanh(t/2) \tag{13} $$

この結果は、多項式の連鎖分割から生成される離散的な漸化式が、連続的な解析関数である双曲線正接関数の恒等式と同値であることを示している。

## 第3章：ジェノッキ数を媒介とする代数的同値性と偽陽性の分離 (Chapter 3: Algebraic Equivalence via Genocchi Numbers and Isolation of False Positives)

本章では、第2章で得られた母関数 $\tilde{A}(t^2)$ を組合せ論的な流路理論の観点から再解釈し、ベルヌーイ数およびジェノッキ数との代数的な接続を定式化する。また、剰余環における非正則性の判定条件と、特定の条件下で発生する偽陽性の論理構造を明らかにする。

### 3.1 流路理論による係数 $R_{2m}$ の組合せ論的定式化

第2章で定義した有限べき乗和（式1）の連鎖分割（式2）は、高次から低次への係数遷移（カスケード）として記述される。この遷移は、有向非巡回グラフ（DAG）上において、出発点となる次数 $c$ から各偶数次基底 $2m$ へ至る経路（パス）の重み付き和としてモデル化される。これを「流路理論」と呼ぶ。

中継ノードが奇数次に制限されるという「パリティ制限」の下で、各ステップの落差 $d_j$ の階乗に初期状態の階乗 $c!$ を分配する多項係数を適用すると、基底係数 $R_{2m}$ は以下の多項係数和として表される。

$$ R_{2m} = - L_c\binom{c}{2m} (c-2m)! \sum_{r=1} \left( -\frac{1}{2} \right)^r \sum_{\substack{d_1 + \dots + d_r = c - 2m \\ d_1, \dots, d_{r-1} \in \{2, 4, \dots\} \\ d_r \in \{1, 3, \dots\}}} \frac{1}{d_1! d_2! \dots d_r!} \tag{14} $$

式(12)の右辺におけるシグマ項は、第2章で示した母関数の展開係数と一致する。解析的には、この経路の総和は双曲線正接関数 $\tanh(t/2)$ のマクローリン展開へ収束し、その係数はジェノッキ数 $G_k$ を用いて記述される。

### 3.2 ベルヌーイ数およびジェノッキ数との接続

$\tanh(z)$ の展開式と式(12)を比較することで、基底係数 $R_{2m}$ は、インデックス $k = c - 2m + 1$ に対応するジェノッキ数 $G_k$ を用いて以下の公式として導出される。

$$ R_{2m} = - \frac{L_c \binom{c}{2m} G_k}{k \cdot 2^{k-1}} \tag{15} $$

ジェノッキ数とベルヌーイ数 $B_k$ の間には $G_k = 2(1 - 2^k) B_k$ の関係がある。これを代入すると、本手法における判定対象である $R_{2m}$ とベルヌーイ数の関係式が得られる。

$$ R_{2m} = - \frac{L_c \binom{c}{2m} (1 - 2^k) B_k}{k \cdot 2^{k-2}} \tag{16} $$

式(16)は、離散的な多項式演算の結果である $R_{2m}$ が、数論的定数であるベルヌーイ数の分子の情報を保持していることを示している。

### 3.3 非正則性の判定と同値性

剰余環 $\mathbb{Z}/p\mathbb{Z}$ 上において、式(16)に基づく判定条件は以下のように分類される。

1.  **通常条件 ($2^k \not\equiv 1 \pmod p$):**
    $1 - 2^k$ が $p$ で割り切れないため、以下の同値関係が成立する。
    $$ B_k \equiv 0 \iff R_{2m} \equiv 0 \pmod p \tag{17} $$
    これにより、$R_{2m}$ の零判定によって非正則素数を評価可能となる。

2.  **例外条件 ($2^k \equiv 1 \pmod p$):**
    この条件下では、$B_k$ の値にかかわらず $R_{2m} \equiv 0 \pmod p$ となり、数論的な偽陽性（False Positive）が発生する。

### 3.4 偽陽性の分離と次元降下

例外条件における非正則性（$B_k \equiv 0 \pmod p$）を評価するためには、剰余環の精度を拡張する必要がある。

例外条件における $p$-進付値を $v = v_p(1 - 2^k)$ とする。式(16)より、法 $p^n$ における $R_{2m}$ の計算結果は、精度を $v$ 次分喪失する。したがって、高次 $p$-進展開係数を抽出するためには、評価空間を $\mathbb{Z}/p^{n-v}\mathbb{Z}$ へと次元降下させる必要がある。本手法は、計算過程で $v > 0$ を検知した際、この次元降下を実行することで、特異点においても非正則性の判定を維持する。

## 第4章：剰余環上の数論変換アルゴリズムと次元降下 (Chapter 4: Number Theoretic Transform Algorithm over Residue Rings and Dimensional Descent)

本章では、前章までに定式化された形式的べき級数（FPS）の代数構造を計算機上に実装し、剰余環 $\mathbb{Z}/p^n\mathbb{Z}$ 上において時間計算量 $O(p \log p)$ で非正則素数を評価するアルゴリズムを構築する。

### 4.1 ニュートン法による形式的べき級数の逆元計算

式(8)における係数配列 $\tilde{A}$ は、形式的べき級数の乗算 $\tilde{A}(x) \equiv \tilde{C}(x) \tilde{B}^{-1}(x) \pmod{x^{N+1}}$ によって算出される。ここで $N = (p-3)/2$ である。
配列 $\tilde{B}$ の逆元 $\tilde{B}^{-1}(x)$ を求めるため、ニュートン法を適用する。関数 $f(Y) = Y^{-1} - \tilde{B}(x)$ に対する反復式は以下のように与えられる。

$$ Y_{j+1} \equiv Y_j (2 - \tilde{B}(x) Y_j) \pmod{x^{2^j}} \tag{18} $$

初期値 $Y_0 \equiv \tilde{B}_0^{-1} \pmod{p^n}$ から開始し、配列の次数を倍加させながら反復計算を行うことで、目的の次数 $N+1$ までの逆元配列を得る。

### 4.2 任意模数数論変換 (Arbitrary Modulo NTT) の適用

ニュートン法および最終的な配列の乗算 $\tilde{C}(x) \tilde{B}^{-1}(x)$ において、多項式の乗算を高速化するために数論変換（NTT）を利用する。素朴な多項式乗算は $O(N^2)$ の時間計算量を要するが、NTTを用いることで $O(N \log N)$ に削減される。

しかし、評価対象となる剰余環 $\mathbb{Z}/p^n\mathbb{Z}$ は、一般にNTTが要請する素数の条件を満たさない。そこで、複数の異なるNTTフレンドリー素数 $q_1, q_2, \dots, q_m$ を用いる任意模数NTT（Garnerのアルゴリズム）を導入する。

各素数 $q_j$ の空間で独立にNTTによる畳み込みを実行し、得られた結果に対して中国剰余定理（CRT）を適用することで、有理数演算を経由せずに真の整数値を復元する。復元された値に対し、最後に法 $p^n$ の剰余をとることで、剰余環上での多項式乗算が成立する。

### 4.3 次元降下ロジックと $p$-進展開係数の抽出

得られた配列 $\tilde{A}$ の各要素 $\tilde{A}_K$ から基底係数 $R_{c-2K-1}$ を算出し、式(16)に基づく判定を行う。インデックス $k = c-2K+1$ において例外条件 $2^k \equiv 1 \pmod p$ が検出された場合、以下の次元降下手順を実行する。

1.  $1 - 2^k$ の $p$-進付値 $v = v_p(1 - 2^k)$ を計算する。
2.  評価空間の法を $p^n$ から $p^{n-v}$ へと降下させる。
3.  降下された法 $p^{n-v}$ の下で、関係式から $B_k \pmod{p^{n-v}}$ の値を再評価する。

この次元降下により偽陽性が排除される。非正則と判定された場合（$B_k \equiv 0 \pmod p$）、計算された値から高次 $p$-進展開係数 $A_0, A_1, \dots, A_{n-v-1}$ を以下の形式で抽出する。

$$ B_k \equiv A_0 + A_1 p + A_2 p^2 + \dots + A_{n-v-1} p^{n-v-1} \pmod{p^{n-v}} \tag{19} $$

これにより、有理数演算に起因する計算不能状態を回避し、決定論的に $p$-進展開係数を取得するアルゴリズムが完結する。全体の計算量は、多項式の次数 $N \approx p/2$ に対するNTTの演算に支配されるため、$O(p \log p)$ となる。

## 第5章：計算機的実証と数値解析 (Chapter 5: Computational Verification and Numerical Analysis)

本章では、構築したアルゴリズムの計算機への実装結果を示し、次元降下ロジックの動作および $O(p \log p)$ エンジンの実行性能を評価する。

### 5.1 特異点における次元降下の動作検証

例外条件による次元降下機構の妥当性を検証するため、ウィーフェリッヒ素数（Wieferich prime）である $p = 1093$ を対象に、法 $p^3$ 空間における評価を実行した。

$p = 1093$ は正則素数（regular prime）であるが、特定のインデックス（$k=364$）において $1 - 2^{1092} \equiv 0 \pmod{p^2}$ を満たすため、$p$-進付値 $v = 2$ の特異点を持つ。単純な剰余演算ではこの箇所で偽陽性が発生する。

本アルゴリズムによる実行結果は、基底総数 $N = 545$ に対し、総計算時間 0.503 秒で非正則ペアの検出数 0 個となった。これにより、計算過程で次元降下（法 $p^3 \to p^1$）が機能し、偽陽性を排除して正則素数として判定されることが確認された。

### 5.2 実行性能と $p$-進展開係数の抽出

本手法の計算スケーラビリティと高次空間での係数抽出能力を評価するため、素数 $p = 100069$ を対象に法 $p^3$ 空間でのスキャンを実行した。この評価における基底総数は $N = 50033$ である。

実験の結果、任意模数NTTおよびニュートン法による全配列の畳み込み計算と判定プロセスが、総計算時間 77.939 秒で完了した。高次モジュロ空間の大規模な評価において、時間計算量 $O(p \log p)$ による処理速度が実証された。

また、本評価により3つの非正則ペアが特定され、法 $p^3$ 空間における $p$-進展開係数 $(A_1, A_2)$ が抽出された（ただし $A_0 = 0$）。

* $k = 16178 \implies B_{16178} \equiv 88100 p + 36965 p^2 \pmod{p^3}$
* $k = 24466 \implies B_{24466} \equiv 60037 p + 3710 p^2 \pmod{p^3}$
* $k = 75486 \implies B_{75486} \equiv 60468 p + 68674 p^2 \pmod{p^3}$

本結果は、有理数の計算機的制約を回避し、決定論的に高次 $p$-進展開係数が取得可能であることを示している。

## 第6章：結論 (Chapter 6: Conclusion)

本論文では、多項式差分の連鎖分割から得られる漸化式の解析的構造を解明し、それに準拠する非正則素数の評価手法および時間計算量 $O(p \log p)$ のアルゴリズムを提案した。

理論面においては、有限べき乗和の係数遷移が双曲線関数の恒等式に対応することを示し、漸化式の計算を形式的べき級数の畳み込みへ帰着させた。この構造により、ベルヌーイ数の有理数演算に起因する計算機的制約を回避する代数基盤が構築された。

アルゴリズム面においては、任意模数数論変換（NTT）を剰余環 $\mathbb{Z}/p^n\mathbb{Z}$ へ適用し、多項式乗算を対数線形時間で処理する機構を実装した。また、特異点において発生する偽陽性を分離するため、$p$-進付値に基づく次元降下ロジックを導入した。

計算機的実証により、本手法が特異点における判定を維持し、従来手法では到達が困難であった巨大素数（$p=100069$）の深層モジュロ空間（法 $p^3$）における高次 $p$-進展開係数を決定論的に抽出可能であることを確認した。本研究は、非正則素数の分布および高次剰余空間における数論的性質の解明に寄与する手法として位置づけられる。

---

## 付録 (Appendix)

### 付録A：任意模数NTTと次元降下アルゴリズムの実装（Python）

本アルゴリズムの中核となる、5つの素数を用いた任意模数NTT（Garnerのアルゴリズム）と、剰余環 $p^n$ 上における次元降下ロジックの実装を示す。
```python
import time

def get_v2(n):
    if n == 0: return 0
    return (n & -n).bit_length() - 1

# =====================================================================
# 5-Prime 任意模数 NTT (Garner's Algorithm)
# =====================================================================
P1, G1 = 167772161, 3
P2, G2 = 469762049, 3
P3, G3 = 998244353, 3
P4, G4 = 1004535809, 3
P5, G5 = 2013265921, 31

M1 = P1
M2 = M1 * P2
M3 = M2 * P3
M4 = M3 * P4

inv_M1_P2 = pow(M1, P2 - 2, P2)
inv_M2_P3 = pow(M2 % P3, P3 - 2, P3)
inv_M3_P4 = pow(M3 % P4, P4 - 2, P4)
inv_M4_P5 = pow(M4 % P5, P5 - 2, P5)

def ntt(a, P, G, invert=False):
    n = len(a)
    res = a[:]
    j = 0
    for i in range(1, n):
        bit = n >> 1
        while j & bit:
            j ^= bit
            bit >>= 1
        j ^= bit
        if i < j:
            res[i], res[j] = res[j], res[i]

    length = 2
    while length <= n:
        half = length // 2
        wlen = pow(G, (P - 1) // length, P)
        if invert:
            wlen = pow(wlen, P - 2, P)
        for i in range(0, n, length):
            w = 1
            for j in range(half):
                u = res[i + j]
                v = (res[i + j + half] * w) % P
                res[i + j] = (u + v) % P
                res[i + j + half] = (u - v) % P
                w = (w * wlen) % P
        length *= 2

    if invert:
        inv_n = pow(n, P - 2, P)
        for i in range(n):
            res[i] = (res[i] * inv_n) % P
    return res

def poly_mul_5prime(A, B, mod_space):
    if not A or not B: return []
    n = 1
    while n < len(A) + len(B) - 1:
        n *= 2

    A_pad = A + [0] * (n - len(A))
    B_pad = B + [0] * (n - len(B))

    A1, B1 = ntt(A_pad, P1, G1), ntt(B_pad, P1, G1)
    C1 = ntt([(a * b) % P1 for a, b in zip(A1, B1)], P1, G1, True)

    A2, B2 = ntt(A_pad, P2, G2), ntt(B_pad, P2, G2)
    C2 = ntt([(a * b) % P2 for a, b in zip(A2, B2)], P2, G2, True)

    A3, B3 = ntt(A_pad, P3, G3), ntt(B_pad, P3, G3)
    C3 = ntt([(a * b) % P3 for a, b in zip(A3, B3)], P3, G3, True)

    A4, B4 = ntt(A_pad, P4, G4), ntt(B_pad, P4, G4)
    C4 = ntt([(a * b) % P4 for a, b in zip(A4, B4)], P4, G4, True)

    A5, B5 = ntt(A_pad, P5, G5), ntt(B_pad, P5, G5)
    C5 = ntt([(a * b) % P5 for a, b in zip(A5, B5)], P5, G5, True)

    res = [0] * (len(A) + len(B) - 1)
    for i in range(len(res)):
        v1 = C1[i]
        t2 = (C2[i] - v1) * inv_M1_P2 % P2
        val2 = v1 + t2 * M1

        t3 = (C3[i] - val2 % P3) * inv_M2_P3 % P3
        val3 = val2 + t3 * M2

        t4 = (C4[i] - val3 % P4) * inv_M3_P4 % P4
        val4 = val3 + t4 * M3

        t5 = (C5[i] - val4 % P5) * inv_M4_P5 % P5
        val5 = val4 + t5 * M4

        res[i] = val5 % mod_space
    return res

def poly_inv(poly, mod_space, length):
    I = [pow(poly[0], -1, mod_space)]
    L = 1
    while L < length:
        L *= 2
        B_sub = poly[:min(len(poly), L)]
        mult1 = poly_mul_5prime(B_sub, I, mod_space)
        temp = [(-x) % mod_space for x in mult1[:L]]
        temp[0] = (temp[0] + 2) % mod_space
        I = poly_mul_5prime(I, temp, mod_space)[:L]
    return I[:length]

# =====================================================================
# メインエンジン: p^3 空間での A0, A1, A2 抽出と次元降下
# =====================================================================
def analyze_irregular_primes(p, power=3):
    start_time = time.time()
    mod_space = p ** power
    c = p - 2
    N = (c - 1) // 2

    fact = [1] * (c + 2)
    for i in range(2, c + 2):
        fact[i] = (fact[i-1] * i) % mod_space
    inv_fact = [0] * (c + 2)
    inv_fact[c+1] = pow(fact[c+1], -1, mod_space)
    for i in range(c, -1, -1):
        inv_fact[i] = (inv_fact[i+1] * (i + 1)) % mod_space
    L_c = 2 ** get_v2(c + 1)
    inv_Lc = pow(L_c, -1, mod_space)

    B_tilde = [0] * (N + 1)
    B_tilde[0] = 2
    for i in range(1, N + 1):
        B_tilde[i] = inv_fact[2 * i]

    C_tilde = [0] * (N + 1)
    for M in range(0, N + 1):
        C_tilde[M] = (L_c * fact[c] * inv_fact[2 * M + 1]) % mod_space

    I_tilde = poly_inv(B_tilde, mod_space, N + 1)
    A_tilde = poly_mul_5prime(C_tilde, I_tilde, mod_space)[:N + 1]

    irregular_count = 0
    for M in range(0, N + 1):
        m = N - M
        two_m = 2 * m
        k = c - two_m + 1
        if k > p - 3: continue

        R_2m = (A_tilde[M] * inv_fact[two_m]) % mod_space
        comb_c_2m = (fact[c] * inv_fact[two_m] * inv_fact[c - two_m]) % mod_space

        G_k_mod = (-k * R_2m * inv_Lc * pow(comb_c_2m, -1, mod_space)) % mod_space
        denom = (2 * (1 - pow(2, k, mod_space))) % mod_space

        # p-進付値の計算と次元降下の判定
        vp = 0
        temp = denom
        while temp % p == 0 and temp > 0:
            vp += 1
            temp //= p

        if vp == 0:
            B_k_mod = (G_k_mod * pow(denom, -1, mod_space)) % mod_space
            A0 = B_k_mod % p

            if A0 == 0:
                A1 = (B_k_mod // p) % p
                A2 = (B_k_mod // p**2) % p
                irregular_count += 1
        else:
            reduced_power = power - vp
            if reduced_power > 0:
                reduced_mod = p ** reduced_power
                denom_prime = (denom // (p ** vp)) % reduced_mod
                G_k_prime = (G_k_mod // (p ** vp)) % reduced_mod
                B_k_reduced = (G_k_prime * pow(denom_prime, -1, reduced_mod)) % reduced_mod

                A0 = B_k_reduced % p
                if A0 == 0:
                    A1 = (B_k_reduced // p) % p if reduced_power >= 2 else 0
                    A2 = (B_k_reduced // p**2) % p if reduced_power >= 3 else 0
                    irregular_count += 1

    elapsed = time.time() - start_time
    return irregular_count, elapsed

if __name__ == "__main__":
    analyze_irregular_primes(100069, power=3)
```

---

## 参考文献 (References)

[1] Washington, L. C. (1997). *Introduction to Cyclotomic Fields* (2nd ed.). Springer.
[2] Kummer, E. E. (1850). "Allgemeiner Beweis des Fermat'schen Satzes...". *Journal für die reine und angewandte Mathematik*, 40, 130-138.
[3] von Staudt, K. G. C. (1840). "Beweis eines Lehrsatzes, die Bernoullischen Zahlen betreffend." *Journal für die reine und angewandte Mathematik*, 21, 372-374.
[4] Clausen, T. (1840). "Lehrsatz aus einer Abhandlung über die Bernoullischen Zahlen." *Astronomische Nachrichten*, 17(22), 351-352.
[5] Knuth, D. E. (1993). "Johann Faulhaber and sums of powers." *Mathematics of Computation*, 61(203), 277-294.
[6] Comtet, L. (1974). *Advanced Combinatorics: The Art of Finite and Infinite Expansions*. Springer.
[7] Cohen, H. (1993). *A Course in Computational Algebraic Number Theory*. Springer.
[8] Buhler, J. P., Crandall, R. E., Ernvall, R., & Metsänkylä, T. (1993). "Irregular primes and cyclotomic invariants to four million." *Mathematics of Computation*, 61(203), 151-153.
[9] Hart, W., Harvey, D., & Ong, W. (2017). "Irregular primes to two billion." *Mathematics of Computation*, 86(308), 3031-3049.
[10] Harvey, D. (2010). "A multimodular algorithm for computing Bernoulli numbers." *Mathematics of Computation*, 79(272), 2361-2370.
[11] Kellner, B. C. (2007). "On irregular prime power divisors of the Bernoulli numbers." *Mathematics of Computation*, 76(257), 405-441.