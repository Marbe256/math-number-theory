# 多項式差分の連鎖分割理論と普遍定数：階層推移方程式から導かれるジェノッキ数の構造
(Linked Partition Theory of Polynomial Differences and Universal Constants: The Structure of Genocchi Numbers Derived from Horizontal Shift Equations)

## 概要
本稿では、多項式の差分構造を対称基底で展開する「連鎖分割理論（LPT）」において、階層係数の新たな算出構造とその代数的帰結を提示する。従来、階層係数は定義に基づく漸化式により順次算出されていたが、本研究は階層間に存在するスケーリング不変性を見出し、任意の階層から2次高い階層の係数を直接導出する有理整数環上の推移方程式を定式化した。さらに、この推移構造から独立する最高次係数について、次元拡張に伴う境界条件によって一意に決定されるメカニズムを明らかにする。
本研究は、この階層間推移を支配する普遍定数が $C(k) = -G_k/k$（ $G_k$ はジェノッキ数）として記述されることを示す。最高次係数を決定する境界条件を解析した結果、ジェノッキ数の漸化式 $\sum_{k=0}^n \binom{n}{k} G_k + G_n = 0$ が離散的な差分構造から自律的に導出されることを証明する。この事実は、多項式差分の境界における対称性の修復が、指数型母関数の解析的構造やベルヌーイ多項式における $x=1/2$ の空間的対称性と代数的に同値であることを意味する。本理論の構築により、ベルヌーイ数特有の有理数体上の特異性が回避され、ディリクレ・エータ関数への正則化に対応する完全な整係数構造が示される。

---

### 1. 序論 (Introduction)

有限べき乗和 $S_{r}(n) = \sum_{j=1}^{n-1} j^r$ の公式化は、数論における基礎的な課題である。古典的な定式化において、べき乗和は次のように展開される。

$$ S_{r}(n) = \frac{1}{r+1} \sum_{k=0}^{r} \binom{r+1}{k} B_k n^{r+1-k} \quad \dots (1) $$

ここで、係数 $B_k$ はベルヌーイ数であり、指数型母関数 $\frac{t}{e^t-1}$ の展開係数として定義される。しかし、この解析的定義は、次数 $r$ が増大するにつれて二項係数とベルヌーイ数が複雑に干渉し、算出コストを増大させる。特にフォン・シュタウト＝クラウゼンの定理が示すベルヌーイ数の分母の性質は、有理数体 $\mathbb{Q}$ 上の演算において特異点となり、アルゴリズムの複雑化を招く。

本論文で記述する多項式差分の連鎖分割理論（Linked Partition Theory: LPT）は、多項式の差分構造を階層 $c$ として捉え、対称性の維持という幾何学的要請からこれらの定数を離散空間上で自律的に生成する体系である。本稿では、この定式化に基づく階層間推移と境界条件の代数構造を解析し、推移を支配する普遍定数がジェノッキ数に帰着することを示す。さらに、離散的な境界における対称性修復のプロセスが、ベルヌーイ多項式における $x=1/2$ を中心とする空間的対称性、およびディリクレ・エータ関数への正則化と代数的に同値であることを証明し、特異点を完全に排除した有理整数環 $\mathbb{Z}$ 上の構造を明らかにする。

### 2. 理論の定式化 (Formulation of LPT)

#### 2.1 対称基底展開
一般化された有限べき乗和を以下のように定義する。

$$ S(a, b, n) = \sum_{j=1}^{n-1} j^a (n-j)^b \quad \dots (2) $$

LPTでは、奇数次の非対称な差分 $c$ を、対称性を持つ偶数次の基底項（差分 $2m$）の線形結合として展開する。

$$ L_c S(a, a+c, n) = \sum_{m=0}^{\frac{c-1}{2}} R_{2m}^{(c)} n^{c-2m} S(a, a+2m, n) \quad \dots (3) $$

ここで $L_c$ は階層 $c$ における正規化定数であり、慣例的に $2^c$ 等の $2$ のべき乗が選択される。これにより、展開係数が整数値として定まる基盤が構築される。

#### 2.2 階層係数の漸化式
係数 $R_{2m}^{(c)}$ は、対象多項式の反対称性条件から、以下の漸化式によって決定される。

$$ R_{2m}^{(c)} = \frac{1}{2} \left( L_c \binom{c}{2m} - \sum_{j=m+1}^{\frac{c-1}{2}} R_{2j}^{(c)} \binom{2j}{2m} \right) \quad \dots (4) $$

この漸化式は、同一階層内において高次の係数から低次の係数を逐次的に求める垂直的な算出構造を持つ。

### 3. 階層間推移方程式 (Horizontal Shift Equation)

#### 3.1 階層間のスケーリング不変性と数論的定数
階層 $c$ の係数 $R_{2m}^{(c)}$ と階層 $c+2$ の係数 $R_{2m+2}^{(c+2)}$ の間には、最高次からの相対的な深さ $k = c - 2m + 1$ において、以下のスケーリング不変性が成立する。

$$ \frac{R_{2m}^{(c)}}{L_c \binom{c}{2m}} = \frac{R_{2m+2}^{(c+2)}}{L_{c+2} \binom{c+2}{2m+2}} = C(k) \quad \dots (5) $$

この普遍定数 $C(k)$ は、階層の深さに依存しない幾何学的な相似比を示す。定数 $C(k)$ は数論的定数であるベルヌーイ数 $B_k$ およびジェノッキ数 $G_k = 2(1 - 2^k)B_k$ と以下の関係を持つ。

$$ C(k) = - \frac{2(1 - 2^k) B_k}{k} = - \frac{G_k}{k} \quad \dots (6) $$

式(6)により、多項式の幾何学的な相似比 $C(k)$ の内部構造が、ベルヌーイ数の総和を経由することなく、ジェノッキ数を核とする数列として一意に記述されることが示される。

#### 3.2 水平推移の定式化
式(5)より、階層 $c$ の係数配列から階層 $c+2$ の係数配列を直接算出する方程式が導出される。

$$ R_{2m+2}^{(c+2)} = R_{2m}^{(c)} \left( \frac{L_{c+2}}{L_c} \right) \frac{(c+2)(c+1)}{(2m+2)(2m+1)} \quad \dots (7) $$

この水平推移プロセスは、式(4)における低次係数の総和を必要としない。また、分母の連続整数の積と係数比の相殺により、有理整数環 $\mathbb{Z}$ 上の乗除算のみで記述され、外部の数論的定数を参照することなく次層の構造を決定できる。

### 4. 境界条件と対称性の修復 (Boundary Conditions and Symmetry Repair)

#### 4.1 最高次係数の決定
推移方程式（式7）は深さ $k \ge 2$ の係数に対して成立するが、最高次係数 $R_0^{(c+2)}$ （ $m=0$ に該当）はこの方程式によって決定されない。最高次係数は、多項式全体の中心対称性を維持するための以下の境界条件によって一意に決定される。

$$ 2R_0^{(c+2)} = L_{c+2} - \sum_{m=1}^{\frac{c+1}{2}} R_{2m}^{(c+2)} \quad \dots (8) $$

この方程式の左辺に現れる係数 $2$ は、空間の次元拡張時に生じる対称性の欠落を補完するための「幾何学的ポテンシャルの折り返し」として機能する。この境界における特異的な代数構造が、次章で示す数論的定数の自律的な生成メカニズムの起点となる。

### 5. 普遍定数の再評価と多項式空間の代数構造

本章では、境界条件の代数構造を解析し、推移を支配する真の定数がジェノッキ数に帰着することを示す。さらに、LPTがベルヌーイ多項式の空間的対称性と完全に同値であることを証明する。

#### 5.1 境界条件のインデックス方程式への変換
式(8)の展開を一般化するため、便宜上、階層のインデックスを $c+2$ から $c$ に戻し、両辺を $L_c$ で除して比率の方程式として整理する。

$$ \frac{2R_0^{(c)}}{L_c} + \sum_{m=1}^{\frac{c-1}{2}} \frac{R_{2m}^{(c)}}{L_c} = 1 \quad \dots (9) $$

ここで、第3章で定義した深さ $k = c-2m+1$ および普遍定数 $C(k)$ の関係 $\frac{R_{2m}^{(c)}}{L_c} = \binom{c}{2m} C(k)$ を適用する。総和のインデックス $m$ が $1$ から $\frac{c-1}{2}$ まで変化するとき、深さ $k$ は $\{c-1, c-3, \dots, 2\}$ をとる。また、二項係数の性質より $\binom{c}{2m} = \binom{c}{c-k+1} = \binom{c}{k-1}$ が成立するため、式(9)の総和項は $\sum \binom{c}{k-1}C(k)$ に変換される。

最高次係数 $R_0^{(c)}$ （ $m=0$）の深さは $k = c+1$ であり、該当する項は $2 \binom{c}{0} C(c+1) = 2 C(c+1)$ となる。これを式(9)に代入する。

$$ 2 C(c+1) + \sum_{k \in \{2, 4, \dots, c-1\}} \binom{c}{k-1} C(k) = 1 \quad \dots (10) $$

左辺の第1項を $2 C(c+1) = C(c+1) + C(c+1)$ と分割する。 $\binom{c}{c} = 1$ であることから、後者の $C(c+1)$ を $\binom{c}{(c+1)-1} C(c+1)$ と表記し、総和項の $k=c+1$ の成分として内部に吸収させることで、以下のインデックス方程式に再構築される。

$$ C(c+1) + \sum_{k \in \{2, 4, \dots, c+1\}} \binom{c}{k-1} C(k) = 1 \quad \dots (11) $$

式(11)は、多項式空間の境界において最高次係数が2倍の質量を持つという幾何学的な要請を、定数 $C(k)$ の和として記述したものである。

#### 5.2 境界条件からのジェノッキ数漸化式の導出
式(11)に $C(k) = -G_k/k$ を代入する。総和項に対して二項係数の公式 $\binom{c}{k-1} \frac{1}{k} = \frac{1}{c+1} \binom{c+1}{k}$ を適用し、分母の深さ $k$ を消去する。

$$ - \frac{G_{c+1}}{c+1} - \frac{1}{c+1} \sum_{k \in \{2, 4, \dots, c+1\}} \binom{c+1}{k} G_k = 1 \quad \dots (12) $$

両辺に $-(c+1)$ を乗じ、 $G_0 = 0, G_1 = 1$ を用いて整理する。右辺の $-(c+1)$ を相殺するために両辺に $(c+1)$ すなわち $\binom{c+1}{1} G_1$ を加算し、 $n=c+1$ ( $n \ge 2$, 偶数) としてインデックスを $k=0$ から $n$ に拡張すると、次式を得る。

$$ \sum_{k=0}^{n} \binom{n}{k} G_k + G_n = 0 \quad \dots (13) $$

式(13)は、解析的に知られるジェノッキ数の生成漸化式と完全に一致する。これにより、LPTの最高次係数の境界条件から、ジェノッキ数の漸化式が有理整数環上の操作のみで自律的に導出されることが証明された。

#### 5.3 指数型母関数による境界構造の解析的裏付け
ジェノッキ数の指数型母関数 $F(x) = \frac{2x}{e^x + 1} = \sum_{k=0}^{\infty} G_k \frac{x^k}{k!}$ を用い、式(13)の構造を解析的に検証する。母関数の定義式から以下の関係が導出される。

$$ 2x = e^x F(x) + F(x) = \sum_{n=0}^{\infty} \left( \sum_{k=0}^{n} \binom{n}{k} G_k + G_n \right) \frac{x^n}{n!} \quad \dots (14) $$

両辺の展開係数を比較すると、 $n=1$ で $2G_1=2$ より $G_1=1$ が導かれ、 $n \ge 2$ において左辺が $0$ となるため式(13)が成立する。すなわち、 $e^x F(x) + F(x) = 2x$ という関係式は、LPTにおける最高次係数の特異的な境界条件を解析的に記述したものである。

#### 5.4 ベルヌーイ多項式の空間的対称性の発現
式(13)のジェノッキ数をベルヌーイ数 $G_k = 2(1 - 2^k) B_k$ に変換し、次式を得る。

$$ \sum_{k=0}^{n} \binom{n}{k} (1 - 2^k) B_k + (1 - 2^n) B_n = 0 \quad \dots (15) $$

左辺を展開して二つの総和に分割する。

$$ \sum_{k=0}^{n} \binom{n}{k} B_k - \sum_{k=0}^{n} \binom{n}{k} 2^k B_k + (1 - 2^n) B_n = 0 \quad \dots (16) $$

第1項から最高次 $k=n$ を分離し、古典的漸化式 $\sum_{k=0}^{n-1} \binom{n}{k} B_k = 0$ を適用して移項・整理すると、次式が導出される。

$$ \sum_{k=0}^{n} \binom{n}{k} 2^k B_k = 2(1 - 2^{n-1}) B_n \quad \dots (17) $$

式(17)の左辺は、ベルヌーイ多項式 $B_n(x)$ に対し $x=1/2$ を代入した評価式に $2^n$ を乗じた値（ $2^n B_n(1/2)$）に等しい。これにより、LPTの決定アルゴリズムが、ベルヌーイ多項式における $x=1/2$ を中心とする空間的対称性と代数的に同値であることが示された。

### 6. 総括：特異点の排除とエータ関数への正則化

LPTにおけるスケーリング因子 $(1 - 2^k)$ は、リーマン・ゼータ関数 $\zeta(s)$ を特異点を持たないディリクレ・エータ関数 $\eta(s) = (1 - 2^{1-s})\zeta(s)$ へと正則化する変換因子に一致する。負の整数における値 $\zeta(1-k) = -B_k/k$ を用いると、普遍定数 $C(k)$ は次のように記述される。

$$ C(k) = - \frac{2(1 - 2^k) B_k}{k} = 2 \eta(1-k) \quad \dots (18) $$

この正則化の機序により、多項式空間の差分演算において有理数体 $\mathbb{Q}$ 上の特異性（分母の発生）が回避され、第3章で示された有理整数環 $\mathbb{Z}$ 上の推移方程式が成立する。ファウルハーバー公式における離散空間の対称性修復は、ジェノッキ数およびエータ関数への接続を伴う代数構造として総括される。

### 7. 結論 (Conclusion)

本稿では、多項式差分の連鎖分割理論（LPT）を定式化し、その階層間推移および境界条件の代数構造を明らかにした。

階層間推移方程式の定式化により、任意の階層の係数から高次階層の係数を直接算出するアルゴリズムが構築され、その演算が有理整数環 $\mathbb{Z}$ 上で完全に閉じる性質が示された。また、推移を支配する普遍定数を同定した結果、ベルヌーイ数の総和計算を経由することなく、ジェノッキ数によって係数が直接生成される代数構造が証明された。

さらに、多項式空間の次元拡張に伴う境界条件を解析した結果、最高次係数における特異的な代数操作から、ジェノッキ数の生成漸化式が自律的に導出されることが示された。この事実は、指数型母関数の解析的構造や、ベルヌーイ多項式における $x=1/2$ を中心とする空間的対称性と完全に同値である。

本理論は、LPTのスケーリング因子がディリクレ・エータ関数への正則化因子と一致することを示し、有理数体 $\mathbb{Q}$ 上で発生するフォン・シュタウト＝クラウゼンの定理に基づく特異性を、多項式空間の離散的かつ幾何学的な対称性修復によって完全に排除できることを結論づけるものである。

