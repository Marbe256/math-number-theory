# Algebraic Generation of Bernoulli Numbers via Linked Partition Theory of Polynomial Differences: Structural Analysis of Faulhaber's Formula through Layered Coefficients

**多項式差分の連鎖分割理論によるベルヌーイ数の代数的生成：階層係数によるファウルハーバー公式の構造解析**

---

## 概要 (Abstract)

本論文では、多項式差分の連鎖分割理論（Linked Partition Theory of Polynomial Differences, 以下LPT）を用い、有限べき乗和の展開構造を解明する。従来、ファウルハーバーの公式における展開係数は母関数の解析的性質として記述されてきたが、本研究では多項式の中心対称性を維持するための幾何学的制約（境界条件）から、これらの定数が自律的に生成されることを示す。また、階層間推移方程式を導入することで、有理整数環 $\mathbb{Z}$ 上の演算のみで高次の展開係数を決定するアルゴリズムを定式化し、数論的定数が「対称性修復のための幾何学的質量」として機能する機序を明らかにする。

---

## 1. 序論 (Introduction)

有限べき乗和 \(S_{r}(n) = \sum_{j=1}^{n-1} j^r\) の公式化は、数論における基礎的な課題である。古典的な定式化において、べき乗和は次のように展開される。

$$
S_{r}(n) = \frac{1}{r+1} \sum_{k=0}^{r} \binom{r+1}{k} B_k n^{r+1-k} ...(1)
$$

ここで、係数 \(B_k\) はベルヌーイ数であり、母関数 \(\frac{t}{e^t-1}\) の展開係数として定義される。しかし、この解析的定義は、次数 \(r\) が増大するにつれて二項係数とベルヌーイ数の分母が複雑に絡み合い、計算コストを増大させる。特にフォン・シュタウト＝クラウゼンの定理が示す分母の性質は、有理数体 \(\mathbb{Q}\) 上の演算において特異点となる。

本論文で記述する連鎖分割理論（LPT）は、多項式の差分構造を階層 $c$ として捉え、対称性の維持という幾何学的要請からこれらの定数を自律的に生成する体系である。

---

## 2. 理論の定式化 (Formulation of LPT)

### 2.1 対称基底展開
一般化された有限べき乗和を以下のように定義する。

$$
S(a, b, n) = \sum_{j=1}^{n-1} j^a (n-j)^b ... (2)
$$

LPTでは、奇数次の非対称な差分 \(c\) を、対称性を持つ偶数次の基底項（差分 \( 2m \)）の線形結合として展開する。

$$
L_c S(a, a+c, n) = \sum_{m=0}^{\frac{c-1}{2}} R_{2m}^{(c)} n^{c-2m} S(a, a+2m, n) ... (3)
$$

ここで \(L_c\) は階層 \(c\) における正規化定数であり、慣例的に \(2^c\) 等の \(2\) のべき乗が選択される。

### 2.2 階層係数の漸化式
係数 \(R_{2m}^{(c)}\) は、対象多項式の反対称性条件から、以下の漸化式によって決定される。

$$
R_{2m}^{(c)} = \frac{1}{2} \left( L_c \binom{c}{2m} - \sum_{j=m+1}^{\frac{c-1}{2}} R_{2j}^{(c)} \binom{2j}{2m} \right) ... (4)
$$

この漸化式は、高次の係数から低次の係数を逐次的に求める構造を持つ。

---

## 3. 階層間推移方程式 (Horizontal Shift Equation)

### 3.1 階層間のスケーリング不変性と数論的定数
階層 \(c\) の係数 \(R_{2m}^{(c)}\) と階層 \(c+2\) の係数 \(R_{2m+2}^{(c+2)}\) の間には、最高次からの相対的な深さ \(k = c - 2m + 1\) において、以下の等式が成立する。

$$
\frac{R_{2m}^{(c)}}{L_c \binom{c}{2m}} = \frac{R_{2m+2}^{(c+2)}}{L_{c+2} \binom{c+2}{2m+2}} = C(k) ... (5)
$$

この普遍定数 \(C(k\) は、階層に依存しない幾何学的な相似比を示す。ここで、定数 \(C(k\) は数論的定数であるベルヌーイ数 \(B_k\) およびジェノッキ数 \(G_k = 2(1 - 2^k)B_k\) と以下の関係を持つ。

$$
C(k) = - \frac{(1 - 2^k) B_k}{k \cdot 2^{k-2}} = - \frac{G_k}{k \cdot 2^{k-1}} ... (6)
$$

この式(6)により、多項式の幾何学的な相似比 \(C(k\) の内部構造が、ジェノッキ数を核とする数列として記述されることが示される。

### 3.2 水平推移の定式化
式(5)より、階層 \(c\) の係数配列から階層 \(c+2\) の係数配列を直接算出する方程式が導かれる。

$$
R_{2m+2}^{(c+2)} = R_{2m}^{(c)} \left( \frac{L_{c+2}}{L_c} \right) \frac{(c+2)(c+1)}{(2m+2)(2m+1)} ... (7)
$$

この推移プロセスは、有理整数環 \(\mathbb{Z}\) 上の乗除算のみで記述され、外部の数論的定数を参照することなく次層の構造を決定できる。

---

## 4. 境界条件と対称性の修復 (Boundary Conditions and Symmetry Repair)

### 4.1 最高次係数の決定
推移方程式(7)によって決定されない最高次係数 \(R_0^{(c+2)}\) は、多項式全体の中心対称性を維持するための以下の境界条件によって一意に決定される。

$$
2R_0^{(c+2)} = L_{c+2} - \sum_{m=1}^{\frac{c+1}{2}} R_{2m}^{(c+2)} ... (8)
$$

この方程式は、次元拡張時に生じる対称性の欠落を補完するための釣り合いの式として機能する。

### 4.2 ファウルハーバー展開の必然性
ファウルハーバーの公式が示す特定の展開構造は、LPTにおける以下の二段階の代数的操作の帰結として記述される。
1. **二項係数のスケーリング**: 次数拡張に伴う既存基底の再配置（水平推移）。
2. **ベルヌーイ数の配置**: スケーリングによって生じた最高次項の空席を、対称性を保つように埋める修復プロセス。

---

## 5. 結論 (Conclusion)

本研究により、べき乗和の展開構造は多項式空間の階層的拡張と、それに付随する対称性修復という幾何学的要請によって支配されていることが示された。LPTの境界条件はベルヌーイ数の再帰的定義と代数的に同型であり、有理整数環上の演算のみでこれらを自律的に生成可能である。本理論は、解析的な定数を「離散的な多項式空間の対称性を維持するための幾何学的質量」として再定義する視座を提供する。

---

## 付録 (Appendix)

### A.1 境界条件とベルヌーイ数恒等式の同値性証明
階層 \(c\) における境界条件は次式で与えられる。

$$
2R_0^{(c)} = L_c - \sum_{m=1}^{\frac{c-1}{2}} R_{2m}^{(c)} ... (A.1)
$$

基底係数 \(R_{2m}^{(c)}\) は式(5)および式(6)に基づき、一般項として次のように代入される。

$$
R_{2m}^{(c)} = - L_c \binom{c}{2m} \frac{(1 - 2^k) B_k}{k \cdot 2^{k-2}} ... (A.2)
$$

両辺を \(L_c\) で除算し、整理すると以下の等式を得る。

$$
-2 \binom{c}{0} \frac{(1 - 2^{c+1}) B_{c+1}}{(c+1) \cdot 2^{c-1}} = 1 + \sum_{m=1}^{\frac{c-1}{2}} \binom{c}{2m} \frac{(1 - 2^{c-2m+1}) B_{c-2m+1}}{(c-2m+1) \cdot 2^{c-2m-1}} ... (A.3)
$$

左辺は総和における \(m=0\) の項と等価であるため、これを右辺に移行してインデックスを統合する。

$$
0 = 1 + \sum_{m=0}^{\frac{c-1}{2}} \binom{c}{2m} \frac{(1 - 2^{c-2m+1}) B_{c-2m+1}}{(c-2m+1) \cdot 2^{c-2m-1}} ... (A.4)
$$

深さ変数 \(k = c - 2m + 1\) で置換を行う。\(m\) が \(0\) から \((c-1)/2\) まで推移するとき、\(k\) は \(c+1\) から \(2\) までの偶数を取る。

$$
0 = 1 + \sum_{k \in \lbrace 2, 4, \dots, c+1 \rbrace} \binom{c}{c-k+1} \frac{(1 - 2^k) B_k}{k \cdot 2^{k-2}} ... (A.5)
$$

二項係数の対称性 \(\binom{c}{c-k+1} = \binom{c}{k-1}\) および変換式 \(\binom{c}{k-1} = \frac{k}{c+1} \binom{c+1}{k}\) を適用する。

$$
0 = 1 + \frac{1}{c+1} \sum_{k \in \lbrace 2, 4, \dots, c+1 \rbrace} \binom{c+1}{k} \frac{(1 - 2^k) B_k}{2^{k-2}} ... (A.6)
$$

ここで、定数項 \(1\) は \(k=1\) の場合の項と一致する。したがって \(k=1\) を総和に組み込み、共通する分母を払うことで最終的な恒等式に収束する。

$$
\sum_{k \in \lbrace 1, 2, 4, \dots, c+1 \rbrace} \binom{c+1}{k} (1 - 2^k) B_k = 0 ... (A.7)
$$

### A.2 指数型母関数による代数的裏付け
式(A.7)の係数 \((1 - 2^k) B_k\) を持つ母関数 \(F(x)\) を、ベルヌーイ数の母関数 \(B(x) = \frac{x}{e^x - 1}\) を用いて検討する。

$$
F(x) = \sum_{k=0}^{\infty} (1 - 2^k) B_k \frac{x^k}{k!} = \frac{x}{e^x - 1} - \frac{2x}{e^{2x} - 1} = \frac{x}{e^x + 1} ... (A.8)
$$

\(F(x) = \frac{x}{e^x + 1}\) はジェノッキ数の母関数の定数倍であり、展開項に \(x^1\) および偶数次の項のみが現れる。これはLPTが対称基底を用いて非対称性を記述することの代数的な整合性を示している。

### A.3 全インデックス範囲への拡張と古典的漸化式の包含
ベルヌーイ数の性質（\(k=0\) で \(1-2^0=0\)、\(k \ge 3\) の奇数で \(B_k=0\)）に基づき、式(A.7)の範囲を拡張する。\(n = c+1\) とすると、以下の境界恒等式が得られる。

$$
\sum_{k=0}^{n} \binom{n}{k} (1 - 2^k) B_k = 0 ... (A.9)
$$

この式における係数 \((1 - 2^k)\) を展開し、二つの総和に分割する。

$$
\sum_{k=0}^{n} \binom{n}{k} B_k - \sum_{k=0}^{n} \binom{n}{k} 2^k B_k = 0 ... (A.10)
$$

第一項から最高次 \(k=n\) の項を分離する。

$$
\left( \sum_{k=0}^{n-1} \binom{n}{k} B_k \right) + B_n - \sum_{k=0}^{n} \binom{n}{k} 2^k B_k = 0 ... (A.11)
$$

括弧内は古典的なベルヌーイ数の漸化式であり、\(n \ge 2\) において \(0\) となる。これを整理すると、次層のベルヌーイ数を決定する関係式が得られる。

$$
B_n = \sum_{k=0}^{n} \binom{n}{k} 2^k B_k ... (A.12)
$$

この展開結果は、LPTの境界条件が古典的な漸化式を内部に包含しつつ、それに階層推移のスケーリング成分 $2^k$ を統合した拡張形として機能していることを示している。

### A.4 総括的考察：ゼータからエータへの相転移
係数 \(1 - 2^k\) はリーマン・ゼータ関数 \( \zeta(s) \) をディリクレ・エータ関数 \( \eta(s) \) へと正則化する因子に対応する。LPTの境界条件は、母関数の特異点を取り除き、対称な正則関数へと修復する正則化と同値である。この転換が、ファウルハーバーの公式における有理数展開の背後にある幾何学的原理である。
