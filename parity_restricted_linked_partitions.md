# 多項式差分のパリティ制限付き連鎖分割：対称べき乗和からジェノッキ数へ
(Parity-Restricted Linked Partitions in Polynomial Differences: From Symmetric Power Sums to Genocchi Numbers)

- 著者：Masatoyo Matsuda
- 日付：2026/02/26

## 概要 (Abstract)
本論文では、対称性を持つべき乗和関数 $S(a,b,n) = \sum_{k=1}^{n-1} k^a (n-k)^b$ を導入し、多項式差分における新たな係数還元アルゴリズム「パリティ制限付き連鎖分割理論」を提唱する。従来のべき乗和の解析とは異なり、本理論は変数の対称性 $S(a,b,n) = S(b,a,n)$ を用いて奇数次項を完全に排除し、偶数次基底への集約（カスケード）を行う点に特徴がある。
本研究では、この高次から低次への係数遷移（流路）が、有向非巡回グラフ（DAG）上における経路の重み付け和としてモデル化できることを示し、その実体が組合せ論における「パリティ（偶奇）制限付きの多項係数和」であることを証明する。さらに、この離散的なネットワーク構造を母関数へ翻訳することで、全経路の総和が双曲線正接関数 $\tanh(t/2)$ のマクローリン展開へと収束することを解明する。結果として、対称べき乗和における終端調整係数が、ジェノッキ数 $G_k$ を用いた簡潔な閉鎖公式として一意に定まることを示す。本研究は、多項式の代数的展開構造と、解析学における双曲線関数および数論的定数の間に存在する数学的接続を明らかにするものである。

---

## 1. 序論 (Introduction)
有限のべき乗和の公式化は、古くから数学における重要なテーマである。自然数の $c$ 乗和は、ヤコブ・ベルヌーイによるファウルハーバーの公式の一般化として、ベルヌーイ数 $B_j$ を用いて以下のように展開されることが広く知られている。

$$
\sum_{k=1}^{n-1} k^c = \frac{1}{c+1} \sum_{j=0}^{c} \binom{c+1}{j} B_j \, n^{c+1-j} \quad \dots (1)
$$

この表現は、 $n$ の多項式としての各係数を静的に決定する点において有用である。しかし、この単一変数の和の形式では、差分作用素が持つ高次から低次への階層的な遷移構造や、区間 $[0, n]$ における和の対称性が明示されない。

本論文では、べき乗和に対する構造的アプローチとして、区間の両端からの距離 $k$ と $(n-k)$ にそれぞれ任意の非負整数 $a, b$ の重みを持たせた「対称べき乗和関数」 $S(a, b, n)$ を新たに導入する。

$$
S(a, b, n) = \sum_{k=1}^{n-1} k^a (n-k)^b \quad \dots (2)
$$

ここで、式(2)において $a=0, b=c$ と置くと、和の順序を逆転させる性質により、従来のべき乗和と一致する。

$$
S(0, c, n) = \sum_{k=1}^{n-1} k^0 (n-k)^c = \sum_{k=1}^{n-1} (n-k)^c = \sum_{k=1}^{n-1} k^c \quad \dots (3)
$$

すなわち、従来のべき乗和は、対称空間 $S(a, b, n)$ における一つの境界条件に該当する。補助変数 $a$ を導入した利点は、定義から導かれる対称性 $S(a, b, n) = S(b, a, n)$ を利用できる点にある。

本研究の目的は、任意の $S(a, a+c, n)$ における二項展開とこの対称性を組み合わせることで、奇数次の項を恒等的に左辺へ折り返し、多項式の空間を「偶数次基底のみ」に制限する係数還元アルゴリズム（パリティ制限付き連鎖分割）を構築することである。さらに、この過程で生じる係数遷移を定式化し、 $a=0$ の条件下において、この離散的なネットワーク計算がジェノッキ数（Genocchi numbers）を生成する解析学的な関数へと収束するメカニズムを証明する。

---

## 2. 対称性と奇数次項の排除 (Symmetry and Elimination of Odd Degrees)
前章で定義した対称べき乗和関数 $S(a, b, n)$ は、和の順序を反転させることにより、以下の対称性を持つ。

$$
S(a, b, n) = \sum_{k=1}^{n-1} k^a (n-k)^b = \sum_{k=1}^{n-1} (n-k)^a k^b = S(b, a, n) \quad \dots (4)
$$

この対称性 $S(a, b, n) = S(b, a, n)$ は、多項式の展開において奇数次の項を消去する操作（折り返し）を可能にする。任意の奇数 $c$ に対し、 $S(a, a+c, n)$ の二項展開を考える。

$$
S(a, a+c, n) = \sum_{k=1}^{n-1} k^a (n-k)^a (n-k)^c \quad \dots (5)
$$

$(n-k)^c$ を二項定理により展開し、和の記号を分配すると以下の式を得る。

$$
S(a, a+c, n) = \sum_{j=0}^{c} (-1)^j \binom{c}{j} n^{c-j} S(a+j, a, n) \quad \dots (6)
$$

ここで、右辺の末尾の項（ $j=c$ のとき）は $(-1)^c \binom{c}{c} n^0 S(a+c, a, n)$ である。 $c$ は奇数であるため $(-1)^c = -1$ となり、項は $-S(a+c, a, n)$ となる。
式(4)の対称性 $S(a+c, a, n) = S(a, a+c, n)$ を適用し、この末尾の項を左辺へ移項することで、左辺は $2S(a, a+c, n)$ となる。

$$
2S(a, a+c, n) = \sum_{j=0}^{c-1} (-1)^j \binom{c}{j} n^{c-j} S(a+j, a, n) \quad \dots (7)
$$

両辺を2で割ることにより、 $S(a, a+c, n)$ は自分自身より低次の項 $S(a+j, a, n)$ の線形結合として表現される。この操作の単純な例として、 $c=1$ の場合を示す。

$$
S(a, a+1, n) = \frac{1}{2} n S(a, a, n) \quad \dots (8)
$$

式(8)は、奇数次である $S(a, a+1, n)$ が、偶数次基底である $S(a, a, n)$ に還元されることを示している。本論文では、この $c=1$ の関係式を起点として、任意の高次奇数 $c$ を偶数次基底へ還元するアルゴリズムを構築する。

---

## 3. 流路理論：有向グラフ上のカスケード計算 (Flow Theory: Cascade Computation on DAG)
式(7)の漸化式的性質を用いると、より高次の $c$ についても低次への還元が可能となる。例えば $c=3$ の場合、展開式から生じる奇数次項 $S(a, a+1, n)$ に式(8)を代入し整理することで、以下の結果を得る。

$$
S(a, a+3, n) = \frac{3}{2} n S(a, a+2, n) - \frac{1}{4} n^3 S(a, a, n) \quad \dots (9)
$$

同様に $c=5$ の場合は、 $S(a, a+3, n)$ および $S(a, a+1, n)$ が生じるが、これらに既知の還元式を順次代入していくことで、奇数次項が消去され、偶数次項のみの線形結合となる。

$$
S(a, a+5, n) = \frac{5}{2} n S(a, a+4, n) - \frac{5}{2} n^3 S(a, a+2, n) + \frac{1}{2} n^5 S(a, a, n) \quad \dots (10)
$$

この代数的な代入・消去のプロセスは、有向非巡回グラフ（DAG: Directed Acyclic Graph）上における「経路の重み付け和（パス・サム）」と数学的に等価であることが示される。これを本稿では「流路理論」と呼ぶ。

式(7)の両辺を2で割った関係式から、出発点の数次 $c$ から下位の次数 $j$ へ遷移する際の1ステップの係数は $\frac{1}{2} (-1)^j \binom{c}{j}$ と定まる。この代数的な性質に基づき、奇数次 $N$ から偶数次基底 $M$ へ至る係数の計算は、以下のルールに従う有向グラフ上の経路（パス）の総和として定式化される。

1. **ノードと落差の定義**: 出発点となる奇数次を $N$、到達点となる次数を $j$ とする。このときの次数の減少量（落差）を $d = N - j$ と定義する。二項係数の対称性 $\binom{N}{j} = \binom{N}{N-j} = \binom{N}{d}$ により、遷移係数の絶対値は常に $\frac{1}{2}\binom{N}{d}$ となる。
2. **パリティによる符号の決定**: 遷移係数の符号は、到達点 $j$ の偶奇（パリティ）に依存する項 $(-1)^j$ によって決定される。
3. **中継ノードの制約**: 式(7)の展開構造上、経路の途中で経由する中継ノードは常に奇数でなければならない。

このルールの適用例として、 $c=7$ の場合の展開 $S(a, a+7, n)$ を考える。各偶数次基底 $M$ （ $M = 6, 4, 2, 0$）へ至る流路とその係数は、以下のように二項係数の連鎖として展開される。

**① 終端 $M=6$ への流路（1通り）**
* $7 \to 6$ (直通):
  $$+\frac{1}{2}\binom{7}{6}$$
* $+\frac{7}{2}$

**② 終端 $M=4$ への流路（2通り）**
* $7 \to 4$ (直通): 
  $$+\frac{1}{2}\binom{7}{4}$$
* $7 \to 5 \to 4$ (1回経由):
  $$\left(-\frac{1}{2}\binom{7}{5}\right)\left(+\frac{1}{2}\binom{5}{4}\right) = -\frac{1}{4}\binom{7}{5}\binom{5}{4}$$
* $+\frac{35}{2} -\frac{105}{4} = -\frac{35}{4}$

**③ 終端 $M=2$ への流路（4通り）**
* $7 \to 2$ (直通):
  $$+\frac{1}{2}\binom{7}{2}$$
* $7 \to 5 \to 2$ および $7 \to 3 \to 2$ (1回経由・2通り):
  $$-\frac{1}{4}\left[ \binom{7}{5}\binom{5}{2} + \binom{7}{3}\binom{3}{2} \right]$$
* $7 \to 5 \to 3 \to 2$ (2回経由・1通り):
  $$\left(-\frac{1}{2}\binom{7}{5}\right)\left(-\frac{1}{2}\binom{5}{3}\right)\left(+\frac{1}{2}\binom{3}{2}\right) = +\frac{1}{8}\binom{7}{5}\binom{5}{3}\binom{3}{2}$$
* $+\frac{21}{2} -\frac{315}{4} +\frac{630}{8}= +\frac{21}{2}$

**④ 終端 $M=0$ への流路（8通り：最終調整係数 $A_7$ に相当）**
* $7 \to 0$ (直通・1通り):
  $$+\frac{1}{2}\binom{7}{0}$$
* $7 \to 5 \to 0, \, 7 \to 3 \to 0, \, 7 \to 1 \to 0$ (1回経由・3通り):
  $$-\frac{1}{4}\left[ \binom{7}{5}\binom{5}{0} + \binom{7}{3}\binom{3}{0} + \binom{7}{1}\binom{1}{0} \right]$$
* $7 \to 5 \to 3 \to 0, \, 7 \to 5 \to 1 \to 0, \, 7 \to 3 \to 1 \to 0$ (2回経由・3通り):
  $$+\frac{1}{8}\left[ \binom{7}{5}\binom{5}{3}\binom{3}{0} + \binom{7}{5}\binom{5}{1}\binom{1}{0} + \binom{7}{3}\binom{3}{1}\binom{1}{0} \right]$$
* $7 \to 5 \to 3 \to 1 \to 0$ (3回経由・1通り):
  $$-\frac{1}{16}\binom{7}{5}\binom{5}{3}\binom{3}{1}\binom{1}{0}$$
* $+\frac{1}{2} -\frac{63}{4} +\frac{420}{8} -\frac{630}{16}= -\frac{17}{8}$

このように、代数的な代入操作の反復は、終端 $M$ に至る有向グラフ上のすべての流路における「連鎖する二項係数の積」として組合せ論的に表現される。したがって、出発点となる奇数次を $N=c$ とし、終端となる偶数次基底 $M=2m$ への経路の総和を $C(c, 2m)$ とおけば、奇数次項 $S(a, a+c, n)$ は最終的に以下の線形結合として定式化される。

$$
S(a, a+c, n) = \sum_{m=0}^{(c-1)/2} C(c,2m) S(a, a+2m, n) \quad \dots (11)
$$

---

## 4. パリティ制限付き連鎖分割定理 (Theorem of Parity-Restricted Linked Partitions)
前章で示した有向グラフ上のカスケード（二項係数の連鎖）は、組合せ論における整数の順序付き分割（Composition）の構造と符合する。

前章の終端 $M=2$ へ至る流路の一つである $7 \to 5 \to 3 \to 2$ の経路に着目する。このパスにおける各ステップの次数減少量（落差）は $d_1=2, d_2=2, d_3=1$ である。この連鎖する二項係数を階乗形式に展開すると以下のようになる。

$$
\binom{7}{5}\binom{5}{3}\binom{3}{2} = \frac{7!}{5!2!} \cdot \frac{5!}{3!2!} \cdot \frac{3!}{2!1!} = \frac{7!}{2! \cdot 2! \cdot 1! \cdot 2!} \quad \dots (12)
$$

式(12)が示す通り、中間ノードの階乗は約分され、初期状態 $7!$ を各ステップの落差の階乗 $d_1!, d_2!, d_3!$ と終端の次数 $M!$ （ここでは $2!$）へと一斉に分配する多項係数（Multinomial coefficient）として統合される。

この性質は、任意の次数と経路長に対して一般化される。奇数次 $N$ から偶数次基底 $M$ へ至る、長さ $r$ （ステップ数 $r$）の経路を考える。この経路は $(r-1)$ 個の中継ノード $K_1, K_2, \dots, K_{r-1}$ を経由する。各ステップの落差を $d_1 = N - K_1, \, d_2 = K_1 - K_2, \dots, d_r = K_{r-1} - M$ とし、全落差は $d_1 + \dots + d_r = N - M$ を満たすものとする。

この経路における二項係数の連鎖は、中継ノード $K$ を用いた表現から落差 $d$ を用いた表現へと変換され、以下のように一つの多項係数として定式化される。

$$
\binom{N}{K_1} \binom{K_1}{K_2} \dots \binom{K_{r-1}}{M} = \binom{N}{N-d_1} \binom{N-d_1}{N-d_1-d_2} \dots \binom{M+d_r}{M} = \frac{N!}{d_1! d_2! \dots d_r! \cdot M!} \quad \dots (13)
$$

中継ノードがすべて奇数であるという流路理論のルール（パリティ制限）により、途中の落差 $d_1, \dots, d_{r-1}$ は奇数から奇数への移動であるため「正の偶数」となり、最後の落差 $d_r$ は奇数から偶数への移動であるため「正の奇数」となる。

奇数次 $N$ から偶数次基底 $M$ へ至るすべての経路の係数の総和を $C(N, M)$ とする。式(13)の性質とパリティ制限を統合し、可能なすべての経路長 $r$ （直通の $r=1$ から最大ステップ数の $m = (N-M+1)/2$ まで）について、重み $\left(-\frac{1}{2}\right)^r$ と共に足し合わせることで、以下の「パリティ制限付き連鎖分割定理」が導出される。

$$
C(N, M) = - \sum_{r=1}^{(N-M+1)/2} \left( -\frac{1}{2} \right)^r \sum_{\substack{d_1 + \dots + d_r = N - M \\ d_1, \dots, d_{r-1} \text{ は正の偶数} \\ d_r \text{ は正の奇数}}} \frac{N!}{d_1! d_2! \dots d_r! \cdot M!} \quad \dots (14)
$$

式(14)は、多項式差分における階層的な係数遷移が、情報を初期状態 $N$ から最終状態 $M$ と経由した各ステップの落差へと一斉に分配する多項係数の和として記述できることを示している。

---

## 5. 母関数とジェノッキ数への相転移 (Generating Functions and Phase Transition to Genocchi Numbers)
前章の式(14)は組合せ論的な定式化であるが、この方程式に解析学的な解釈を与えることで、特定の関数への収束が示される。
式(14)から $\frac{N!}{M!} = \binom{N}{M}(N-M)!$ をくくり出し、終端係数（ $M=0$、すなわち $c$ 次のべき乗和の調整係数 $A_c = C(c, 0)$）に注目する。

$$
A_c = - c! \sum_{r=1}^{(c+1)/2} \left( -\frac{1}{2} \right)^r \sum_{\substack{d_1 + \dots + d_r = c \\ d_i \text{ の偶奇制限}}} \frac{1}{d_1! d_2! \dots d_r!} \quad \dots (15)
$$

式(15)の右辺にある $\sum \frac{1}{d_1! \dots d_r!}$ は、指数型母関数の積の展開係数に該当する。
パリティ制限により、偶数の落差 $d_i$ は関数 $\cosh(t) - 1$ のマクローリン展開に対応し、奇数の落差 $d_r$ は関数 $\sinh(t)$ の展開に対応する。したがって、長さ $r$ の全経路の和は、以下の無限等比級数 $F(t)$ の $t^c / c!$ の係数として抽出される。

$$
F(t) = - \sum_{r=1}^{\infty} \left( -\frac{1}{2} \right)^r (\cosh t - 1)^{r-1} \sinh t \quad \dots (16)
$$

この無限級数を等比数列の和の公式 $\frac{a}{1-R}$ を用いて計算する。

$$
F(t) = \frac{\frac{1}{2}\sinh t}{1 + \frac{1}{2}(\cosh t - 1)} = \frac{\sinh t}{\cosh t + 1} \quad \dots (17)
$$

双曲線関数の半角の公式（ $\sinh t = 2\sinh\frac{t}{2}\cosh\frac{t}{2}$ および $\cosh t + 1 = 2\cosh^2\frac{t}{2}$）を適用すると、式(17)は以下のように相殺される。

$$
F(t) = \tanh\left(\frac{t}{2}\right) \quad \dots (18)
$$

すなわち、ネットワーク上の全経路の総和は、双曲線正接関数 $\tanh(t/2)$ へと収束する。
$\tanh(t/2)$ のマクローリン展開はベルヌーイ数 $B_{2n}$、あるいはジェノッキ数 $G_{2n} = 2(1-2^{2n})B_{2n}$ を用いて記述される。 $c$ が奇数（$c=2n-1$）であるとき、展開係数を比較することで、対称べき乗和の終端調整係数 $A_c$ は以下の閉鎖公式として定まる。

$$
A_c = - \frac{G_{c+1}}{c+1} \quad \dots (19)
$$

本章の展開により、代数的な代入操作の結果として、ベルヌーイ数の派生であるジェノッキ数が現れることが証明された。

---

## 6. 結論と今後の展望 (Conclusion and Future Perspectives)
### 結論
本論文では、対称べき乗和 $S(a, b, n) = \sum k^a (n-k)^b$ における多項式差分の構造を解明し、奇数次項の排除からジェノッキ数へと至る「パリティ制限付き連鎖分割理論」を定式化した。

本研究ではまず、対称性 $S(a, b, n) = S(b, a, n)$ を用いた折り返しの操作により、高次奇数項が連鎖的に低次偶数基底へと還元されるプロセスを記述した。この代数的なカスケード計算は、有向非巡回グラフ上のネットワークモデルへと変換され、最終的にパリティ制限付きの多項係数和として記述されることが証明された。

さらに、母関数の枠組みへ翻訳することで、全経路和が双曲線正接関数 $\tanh(t/2)$ へと収束することを示した。これにより、終端における調整係数 $A_c$ は、ジェノッキ数 $G_{c+1}$ を用いた閉鎖公式 $A_c = -G_{c+1}/(c+1)$ として一意に定まる。本研究の意義は、多項式の代数操作と、組合せ論（整数の分割）および解析学（双曲線関数・ジェノッキ数）を繋ぐ数学的構造を明示した点にある。

### 今後の展望
本理論の確立により、以下の研究展開が期待される。

第一に、計算機アルゴリズムへの応用である。本論文で導出された公式群は、超高次の対称べき乗和を展開する際に有用である。一般に漸化式を解く場合、再帰代入や動的計画法（メモ化）による中間データの保持が必要となり、次数に比例して計算量が増大する。しかし、本理論によれば、任意の中間係数 $C(N, M)$ は式(15)より $C(N, M) = \binom{N}{M} A_{N-M}$ として分離可能であるため、ジェノッキ数を参照することで時間計算量 $O(1)$ で直接算出するアルゴリズムが構築可能となる。

第二に、関数 $S(a, b, n)$ の定義域の拡張である。本稿では $n$ を自然数とした有限和を前提としたが、本理論が解析的な連続関数 $\tanh(t/2)$ へと接続された事実は、$n$ を負の整数、非整数、さらには複素数領域へ拡張できる可能性を示唆している。有限和から無限級数、あるいは積分表現への解析接続を行うことで、リーマンゼータ関数 $\zeta(s)$ における新たな恒等式や、対称べき乗和の複素平面上での性質解明に寄与することが期待される。

---

## 付録 (Appendix)

### 付録A：流路理論によるカスケードの具体例（$A_7$ の経路トレース）
本編第4章で導出した方程式（式14）の機能を、$c=7$ から偶数次基底 $0$ への終端調整係数 $A_7 = C(7, 0)$ の算出を通じて検証する。落差は $N-M = 7-0 = 7$ であり、各経路長 $r$ における多項係数和 $\sum \frac{7!}{d_1! \dots d_r!}$ を計算する。

* **$r=1$ の経路（直通）**
  落差の分割は $d_1=7$ のみ。$\frac{7!}{7!} = 1$。
  重み $\left(-\frac{1}{2}\right)^1$ を適用：**$-1/2$**
* **$r=2$ の経路（偶数 $\to$ 奇数）**
  $d_1 + d_2 = 7$ の組合せは $(2, 5), (4, 3), (6, 1)$。
  $\frac{7!}{2!5!} + \frac{7!}{4!3!} + \frac{7!}{6!1!} = 21 + 35 + 7 = 63$。
  重み $\left(-\frac{1}{2}\right)^2$ を適用：**$63/4$**
* **$r=3$ の経路（偶数 $\to$ 偶数 $\to$ 奇数）**
  $d_1 + d_2 + d_3 = 7$ の組合せは $(2, 2, 3), (2, 4, 1), (4, 2, 1)$。
  $\frac{7!}{2!2!3!} + \frac{7!}{2!4!1!} + \frac{7!}{4!2!1!} = 210 + 105 + 105 = 420$。
  重み $\left(-\frac{1}{2}\right)^3$ を適用：**$-420/8 = -105/2$**
* **$r=4$ の経路（最長カスケード：偶 $\to$ 偶 $\to$ 偶 $\to$ 奇）**
  組合せは $(2, 2, 2, 1)$ のみ。$\frac{7!}{2!2!2!1!} = 630$。
  重み $\left(-\frac{1}{2}\right)^4$ を適用：**$630/16 = 315/8$**

全経路の和をとり、式(14)の負号を適用する。
$$A_7 = - \left( -\frac{1}{2} + \frac{63}{4} - \frac{105}{2} + \frac{315}{8} \right) = - \left( \frac{-4 + 126 - 420 + 315}{8} \right) = -\frac{17}{8}$$
本編の閉鎖公式（式19）において $c=7$（ジェノッキ数 $G_8 = 17$）を用いた結果も $A_7 = -\frac{G_8}{8} = -\frac{17}{8}$ となり、両者が一致することが確認される。

### 付録B：第2種スターリング数との接続
本理論の基盤である多項係数による落差の分割は、一般の多項式差分における第2種スターリング数 $S_2(n, k)$ の生成構造と符合する。
通常の差分作用素において、階数 $k$ が固定された遷移係数は $S_2$ を用いて記述される。第2種スターリング数を特定の重み付きで足し合わせると、以下の恒等式のようにベルヌーイ数 $B_n$ が生成されることが知られている。

$$B_n = \sum_{k=0}^n (-1)^k \frac{k!}{k+1} S_2(n, k)$$

本論文で展開した式(14)は、この恒等式のパリティ制限付き表現に相当する。固定されたステップ数における離散的な「第2種スターリング数（落差の順序付き分割）」が、偶奇の制約を通して全ステップ数で総和された結果、双曲線関数 $\tanh(t/2)$ を経由してジェノッキ数（ベルヌーイ数の派生）へと接続される構造が示される。

### 付録C：中間係数の定数時間算出アルゴリズム
本論文で構築した理論は、最終的な調整係数 $A_c$ だけでなく、展開プロセスにおける任意の中間係数を高速に算出するアルゴリズムとしても機能する。

奇数次 $N$ から偶数次基底 $M$ へ至る全経路の総和 $C(N, M)$ は、パリティ制限付き連鎖分割定理（式14）の構造上、経路の選択（二項係数）と落差 $N-M$ における終端カスケードに完全に分離される。すなわち、任意の中間係数 $C(N, M)$ は、落差を $c' = N-M$ としたときの調整係数 $A_{c'}$ を用いて以下の関係式を満たす。

$$C(N, M) = \binom{N}{M} A_{N-M}$$

この性質と、第5章で導出したジェノッキ数による閉鎖公式（式19）を統合することで、中間係数を算出する以下の一般公式が得られる。

$$C(N, M) = - \binom{N}{M} \frac{G_{N-M+1}}{N-M+1}$$

この公式の有用性を、$c=7$ （すなわち $N=7$）の展開において、中間基底 $S(a, a+2, n)$ （すなわち $M=2$）の係数 $C(7, 2)$ を算出する例で検証する。
公式を適用すると、落差は $7-2 = 5$ となるため、必要なジェノッキ数は $G_6 = -3$ である。

$$C(7, 2) = - \binom{7}{2} \frac{G_6}{6} = - 21 \cdot \frac{-3}{6} = \frac{21}{2}$$

一方、第3章の具体例で示した流路理論による $M=2$ への全経路（直通、1回経由、2回経由）の総和を直接計算すると以下のようになる。

* 直通（1通り）: $+\frac{1}{2}\binom{7}{2} = \frac{21}{2}$
* 1回経由（2通り）: $-\frac{1}{4} \left[ 21 \cdot 10 + 35 \cdot 3 \right] = -\frac{315}{4}$
* 2回経由（1通り）: $+\frac{1}{8} \cdot 21 \cdot 10 \cdot 3 = +\frac{315}{4}$

これらの総和は $\frac{21}{2} - \frac{315}{4} + \frac{315}{4} = \frac{21}{2}$ となり、公式による算出結果と完全に一致する。

この結果は、従来のように動的計画法等を用いて高次から順にすべての中間項を計算・保持することなく、目的の次数の係数をジェノッキ数の参照のみで時間計算量 $O(1)$ にて独立算出できることを証明している。これは計算機代数における極めて強力な最適化手法となる。